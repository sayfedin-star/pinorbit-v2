---
export const prerender = false;
import Layout from '../layouts/Layout.astro';
import SectionHeader from '../components/ui/SectionHeader.astro';
import PinArchiveCronCard from '../components/pinarchive/PinArchiveCronCard.astro';
import { getWorkspaces } from '../lib/workspaces';

const activeWorkspaceId = Astro.locals.activeWorkspaceId || '';
const workspaces = await getWorkspaces(Astro.cookies, Astro.request, Astro.response?.headers);
const currentWorkspace = workspaces.find((w) => w.id === activeWorkspaceId) || workspaces[0];
const isMasterWorkspace = Boolean(currentWorkspace?.is_master);
---

<Layout title="PinArchive Intelligence" description="Historical pin performance, winning recipe clusters, SEO topics, and canonical variations.">
  <div id="pinarchive-page-root" data-workspace-id={activeWorkspaceId} data-is-master={isMasterWorkspace ? 'true' : 'false'}>
  
  <!-- ═══ Header & Primary Controls ═══ -->
  <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between mb-6">
    <div>
      <div class="flex items-center gap-3">
        <span class="inline-flex items-center justify-center rounded-2xl bg-primary/10 p-2.5 text-primary border border-primary/20 shadow-xs">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <rect width="20" height="5" x="2" y="3" rx="1"/><path d="M4 8v11a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8"/><path d="M10 12h4"/>
          </svg>
        </span>
        <div>
          <div class="flex items-center gap-2">
            <h1 class="text-xl font-bold tracking-tight text-foreground">PinArchive Intelligence</h1>
            <span class="inline-flex items-center rounded-full bg-primary/10 px-2.5 py-0.5 text-[11px] font-semibold text-primary border border-primary/20">
              Live Monitor
            </span>
          </div>
          <p class="text-xs text-muted-foreground mt-0.5">
            Historical creator pin archive, high-velocity winners, and real-time synchronization.
          </p>
        </div>
      </div>
    </div>

    <!-- Top Action Group (Uniform h-9 buttons with clean minimalist styling) -->
    <div class="flex flex-wrap items-center gap-2.5 self-start sm:self-auto">
      <!-- Live Sync Status Pill -->
      <div id="refresh-meta-line" class="inline-flex h-9 items-center gap-2 rounded-xl border border-border/80 bg-card px-3 text-xs text-muted-foreground shadow-xs">
        <span class="relative flex h-2 w-2">
          <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-emerald-400 opacity-75"></span>
          <span class="relative inline-flex rounded-full h-2 w-2 bg-emerald-500"></span>
        </span>
        <span id="refresh-meta-text" class="font-medium text-foreground">Loading sync schedule...</span>
      </div>

      <!-- Architecture Guide Trigger Button -->
      <button
        id="open-architecture-guide-btn"
        type="button"
        class="inline-flex h-9 items-center gap-1.5 rounded-xl border border-primary/30 bg-primary/10 text-primary hover:bg-primary hover:text-primary-foreground px-3 text-xs font-semibold shadow-xs transition-all cursor-pointer"
        title="View Architecture & Automation Guide"
      >
        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <circle cx="12" cy="12" r="10"/><path d="M9.09 9a3 3 0 0 1 5.83 1c0 2-3 3-3 3"/><line x1="12" y1="17" x2="12.01" y2="17"/>
        </svg>
        <span>📖 How It Works</span>
      </button>

      <!-- Track Creator / Add Account Button -->
      <button
        id="open-add-creator-modal"
        type="button"
        class="inline-flex h-9 items-center justify-center gap-1.5 rounded-xl bg-primary px-4 text-xs font-semibold text-primary-foreground shadow-sm hover:bg-primary/90 hover:scale-[1.01] active:scale-[0.99] transition-all cursor-pointer"
      >
        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
        <span>Track Creator</span>
      </button>

      <!-- Quick Navigation Toolbar -->
      <div class="flex items-center rounded-xl border border-border/80 bg-card p-0.5 shadow-xs">
        <button id="pa-refresh-btn" title="Refresh PinArchive overview data"
          class="inline-flex h-8 w-8 items-center justify-center rounded-lg text-muted-foreground hover:text-foreground hover:bg-muted/80 transition-all">
          <svg id="pa-refresh-icon" xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12a9 9 0 1 1-2.64-6.36"/><polyline points="21 3 21 9 15 9"/></svg>
        </button>
        <div class="h-4 w-px bg-border/80 my-auto"></div>
        <a
          href="/pinarchive/winning-pins"
          class="inline-flex h-8 items-center gap-1.5 rounded-lg px-2.5 text-xs font-semibold text-foreground hover:bg-muted/80 transition-all"
        >
          <span>🏆 Winning Pins →</span>
        </a>
        <div class="h-4 w-px bg-border/80 my-auto"></div>
        <a
          href="/pinarchive/topics"
          class="inline-flex h-8 items-center gap-1.5 rounded-lg px-2.5 text-xs font-semibold text-foreground hover:bg-muted/80 transition-all"
        >
          <span>🏷️ Topic Clusters →</span>
        </a>
      </div>
    </div>
  </div>

  {isMasterWorkspace && (
    <!-- ═══ 👑 Master Workspace (Global Orchestrator) Banner ═══ -->
    <div class="mb-6 rounded-2xl border border-amber-500/30 bg-gradient-to-r from-amber-500/10 via-amber-500/5 to-transparent p-4 sm:p-5 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4 shadow-xs">
      <div class="flex items-center gap-3.5">
        <div class="flex h-11 w-11 shrink-0 items-center justify-center rounded-2xl bg-amber-500/20 text-amber-600 dark:text-amber-400 border border-amber-500/30 text-xl shadow-2xs">
          👑
        </div>
        <div>
          <div class="flex items-center gap-2">
            <h3 class="text-sm font-bold text-foreground">Master Workspace (Global Multi-Tenant Orchestrator)</h3>
            <span class="rounded-full bg-amber-500/15 border border-amber-500/30 text-amber-600 dark:text-amber-400 px-2.5 py-0.5 text-[10px] font-bold">ALL-WORKSPACES MODE</span>
          </div>
          <p class="text-xs text-muted-foreground mt-0.5">
            This workspace acts as the central command center. Automation dispatches and FastCron schedules configured here run across <strong>all active workspaces</strong> concurrently in parallel via 4 matrix sharding runners.
          </p>
        </div>
      </div>
    </div>
  )}

  <!-- ═══ PinArchive Tab Navigation (Segmented Minimalist Pill Switcher) ═══ -->
  <div class="mb-6 flex items-center gap-1 bg-muted/60 p-1 rounded-2xl border border-border/60 w-fit">
    <button
      id="pa-tab-btn-accounts"
      type="button"
      class="inline-flex items-center gap-2 px-4 py-2 rounded-xl text-xs font-bold bg-background text-foreground shadow-2xs transition-all cursor-pointer"
    >
      <span>👤 Tracked Accounts &amp; Ingest</span>
    </button>
    <button
      id="pa-tab-btn-automation"
      type="button"
      class="inline-flex items-center gap-2 px-4 py-2 rounded-xl text-xs font-bold text-muted-foreground hover:text-foreground transition-all cursor-pointer"
    >
      <span>⚡ Pipeline &amp; Automation</span>
    </button>
  </div>

  <!-- ═══ Tab 1 View: Accounts, KPIs & Ingest Rules ═══ -->
  <div id="pa-tab-view-accounts" class="space-y-6">
  
    <!-- ═══ KPI Dashboard (4 cards with clean micro-borders) ═══ -->
    <div class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-4 gap-4">
      <!-- Card 1: Accounts -->
      <div class="rounded-2xl border border-border/80 bg-card p-4 sm:p-5 shadow-xs hover:border-primary/30 transition-all flex flex-col justify-between group">
        <div class="flex items-center justify-between">
          <span class="text-[10.5px] font-semibold text-muted-foreground uppercase tracking-wider">Archived Accounts</span>
          <span class="flex h-7 w-7 items-center justify-center rounded-xl bg-primary/10 text-primary border border-primary/20 group-hover:scale-105 transition-transform">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
          </span>
        </div>
        <div>
          <div class="text-2xl sm:text-3xl font-bold tracking-tight text-foreground mt-2 font-mono tabular-nums" id="kpi-accounts-count">
            <div class="h-7 w-12 bg-muted/60 rounded animate-pulse"></div>
          </div>
          <div class="mt-2 text-[11px] text-muted-foreground flex items-center gap-1.5" id="kpi-accounts-sub">
            <span class="h-1.5 w-1.5 rounded-full bg-primary"></span>
            <span>Tracked in workspace</span>
          </div>
        </div>
      </div>

      <!-- Card 2: Archived Pins -->
      <div class="rounded-2xl border border-border/80 bg-card p-4 sm:p-5 shadow-xs hover:border-emerald-500/30 transition-all flex flex-col justify-between group">
        <div class="flex items-center justify-between">
          <span class="text-[10.5px] font-semibold text-muted-foreground uppercase tracking-wider">Archived Pins</span>
          <span class="flex h-7 w-7 items-center justify-center rounded-xl bg-emerald-500/10 text-emerald-600 dark:text-emerald-400 border border-emerald-500/20 group-hover:scale-105 transition-transform">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect width="20" height="5" x="2" y="3" rx="1"/><path d="M4 8v11a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8"/><path d="M10 12h4"/></svg>
          </span>
        </div>
        <div>
          <div class="text-2xl sm:text-3xl font-bold tracking-tight text-emerald-600 dark:text-emerald-400 mt-2 font-mono tabular-nums" id="kpi-archived-pins">
            <div class="h-7 w-16 bg-muted/60 rounded animate-pulse"></div>
          </div>
          <div class="mt-2 text-[11px] text-muted-foreground flex items-center gap-1.5" id="kpi-archived-sub">
            <span class="h-1.5 w-1.5 rounded-full bg-emerald-500"></span>
            <span>With verified metrics</span>
          </div>
        </div>
      </div>

      <!-- Card 3: Total Saves -->
      <div class="rounded-2xl border border-border/80 bg-card p-4 sm:p-5 shadow-xs hover:border-rose-500/30 transition-all flex flex-col justify-between group">
        <div class="flex items-center justify-between">
          <span class="text-[10.5px] font-semibold text-muted-foreground uppercase tracking-wider">Total Saves</span>
          <span class="flex h-7 w-7 items-center justify-center rounded-xl bg-rose-500/10 text-rose-600 dark:text-rose-400 border border-rose-500/20 group-hover:scale-105 transition-transform">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M19 14c1.49-1.46 3-3.21 3-5.5A5.5 5.5 0 0 0 16.5 3c-1.76 0-3 .5-4.5 2-1.5-1.5-2.74-2-4.5-2A5.5 5.5 0 0 0 2 8.5c0 2.3 1.5 4.05 3 5.5l7 7Z"/></svg>
          </span>
        </div>
        <div>
          <div class="text-2xl sm:text-3xl font-bold tracking-tight text-foreground mt-2 font-mono tabular-nums" id="kpi-total-saves">
            <div class="h-7 w-20 bg-muted/60 rounded animate-pulse"></div>
          </div>
          <div class="mt-2 text-[11px] text-muted-foreground flex items-center gap-1.5" id="kpi-saves-sub">
            <span class="h-1.5 w-1.5 rounded-full bg-rose-500"></span>
            <span>Cumulative saves</span>
          </div>
        </div>
      </div>

      <!-- Card 4: Total Shares -->
      <div class="rounded-2xl border border-border/80 bg-card p-4 sm:p-5 shadow-xs hover:border-blue-500/30 transition-all flex flex-col justify-between group">
        <div class="flex items-center justify-between">
          <span class="text-[10.5px] font-semibold text-muted-foreground uppercase tracking-wider">Total Shares</span>
          <span class="flex h-7 w-7 items-center justify-center rounded-xl bg-blue-500/10 text-blue-600 dark:text-blue-400 border border-blue-500/20 group-hover:scale-105 transition-transform">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="18" cy="5" r="3"/><circle cx="6" cy="12" r="3"/><circle cx="18" cy="19" r="3"/><line x1="8.59" y1="13.51" x2="15.42" y2="17.49"/><line x1="15.41" y1="6.51" x2="8.59" y2="10.49"/></svg>
          </span>
        </div>
        <div>
          <div class="text-2xl sm:text-3xl font-bold tracking-tight text-foreground mt-2 font-mono tabular-nums" id="kpi-total-shares">
            <div class="h-7 w-16 bg-muted/60 rounded animate-pulse"></div>
          </div>
          <div class="mt-2 text-[11px] text-muted-foreground flex items-center gap-1.5" id="kpi-shares-sub">
            <span class="h-1.5 w-1.5 rounded-full bg-blue-500"></span>
            <span>Engagement signals</span>
          </div>
        </div>
      </div>
    </div>

    <!-- ═══ PinArchive Ingestion & Archive Rules (Modern Bento Visual Rules) ═══ -->
    <section class="rounded-2xl border border-border/80 bg-card/70 backdrop-blur-sm shadow-xs overflow-hidden transition-all duration-200" aria-labelledby="ingest-settings-heading">
      
      <!-- Header Strip -->
      <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3 p-4 sm:px-6 sm:py-3.5 bg-muted/20 border-b border-border/70">
        <div class="flex flex-wrap items-center gap-3">
          <div class="flex h-9 w-9 items-center justify-center rounded-xl bg-primary/10 text-primary border border-primary/20 shadow-2xs">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-4.5 w-4.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"/><path d="m4.93 4.93 4.24 4.24"/><path d="m14.83 9.17 4.24-4.24"/><path d="m14.83 14.83 4.24 4.24"/><path d="m9.17 14.83-4.24 4.24"/><circle cx="12" cy="12" r="4"/>
            </svg>
          </div>
          <div>
            <div class="flex items-center gap-2">
              <h2 id="ingest-settings-heading" class="text-sm font-bold text-foreground">
                Pin Qualification &amp; Ingest Rules
              </h2>
              <span id="settings-summary-badge" class="text-[10px] font-bold px-2.5 py-0.5 rounded-full bg-emerald-500/10 text-emerald-600 dark:text-emerald-400 border border-emerald-500/20">
                🟢 Ingest Active
              </span>
              <span id="settings-role-badge" class="hidden text-[10px] font-semibold px-2 py-0.5 rounded-full bg-muted text-muted-foreground">
                Admin Only
              </span>
            </div>
            <p class="text-[11px] text-muted-foreground mt-0.5">
              Multi-tier criteria to automatically qualify, rank, and archive winning creator pins.
            </p>
          </div>
        </div>

        <div class="flex items-center gap-2 self-start sm:self-auto flex-wrap">
          <span id="settings-status-msg" class="text-xs font-semibold"></span>
          <button
            id="reevaluate-candidates-btn"
            type="button"
            class="inline-flex h-9 items-center gap-1.5 rounded-xl border border-border/80 bg-background hover:bg-muted text-foreground px-3.5 text-xs font-semibold shadow-2xs hover:border-primary/40 transition-all disabled:opacity-50 disabled:cursor-not-allowed cursor-pointer"
            title="Re-evaluate candidate pins against current rules"
          >
            <svg id="reevaluate-icon" xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12a9 9 0 1 1-2.64-6.36"/><polyline points="21 3 21 9 15 9"/></svg>
            <span id="reevaluate-btn-text">Re-evaluate Candidates</span>
          </button>
          <button
            id="save-ingest-settings-btn"
            type="button"
            class="inline-flex h-9 items-center gap-1.5 rounded-xl bg-emerald-600 hover:bg-emerald-500 text-white px-4 text-xs font-bold shadow-xs hover:shadow-md transition-all disabled:opacity-50 disabled:cursor-not-allowed cursor-pointer ring-1 ring-emerald-400/30"
          >
            <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/><polyline points="17 21 17 13 7 13 7 21"/><polyline points="7 3 7 8 15 8"/></svg>
            <span>Save Rules</span>
          </button>
          <button
            id="toggle-settings-collapse-btn"
            type="button"
            class="inline-flex h-9 items-center gap-1.5 text-xs font-semibold text-muted-foreground hover:text-foreground bg-muted/60 hover:bg-muted px-3 rounded-xl transition-colors cursor-pointer"
            title="Toggle settings visibility"
          >
            <span id="settings-collapse-text">Configure</span>
            <svg id="settings-collapse-icon" xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5 transition-transform duration-200" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="6 9 12 15 18 9"/></svg>
          </button>
        </div>
      </div>

      <!-- Modern Collapsible Form Body -->
      <div id="settings-panel-body" class="hidden p-5 sm:p-6 bg-card space-y-5">
        
        <!-- ═══ 2-Column Bento Grid: Qualification Criteria (Left) + Automation Routines (Right) ═══ -->
        <div class="grid grid-cols-1 lg:grid-cols-12 gap-5">
          
          <!-- LEFT CARD (col-span-7): Single Qualification Criteria Card with 3 Polished Tier Rows -->
          <div class="lg:col-span-7 rounded-2xl border border-border/80 bg-gradient-to-b from-card via-card to-background/50 p-4.5 sm:p-5 shadow-xs flex flex-col justify-between space-y-4">
            <div>
              <div class="flex items-center justify-between pb-3 border-b border-border/60 mb-3.5">
                <div class="flex items-center gap-2">
                  <span class="text-xs font-bold text-foreground">🎯 Qualification Criteria (OR Rules)</span>
                  <span class="text-[10px] text-muted-foreground font-medium">Automatic pin preservation thresholds</span>
                </div>
                <span class="text-[10px] font-bold px-2.5 py-0.5 rounded-full bg-primary/10 text-primary border border-primary/25">
                  OR Logic
                </span>
              </div>

              <!-- 3 Stacked Tier Rows -->
              <div class="space-y-3">
                
                <!-- Row 1: Star Performers (Min Saves) -->
                <div class="flex items-center justify-between gap-3 p-3 rounded-xl border border-emerald-500/25 bg-emerald-500/5 hover:border-emerald-500/45 transition-all group">
                  <div class="flex items-center gap-3 min-w-0">
                    <div class="flex h-8 w-8 shrink-0 items-center justify-center rounded-xl bg-emerald-500/15 text-emerald-600 dark:text-emerald-400 text-sm font-bold border border-emerald-500/25 shadow-2xs group-hover:scale-105 transition-transform">
                      🌟
                    </div>
                    <div class="min-w-0">
                      <div class="flex items-center gap-2">
                        <span class="text-xs font-bold text-foreground truncate">Star Performers (Saves)</span>
                        <span class="text-[9px] font-bold uppercase tracking-wider px-1.5 py-0.2 rounded bg-emerald-500/10 text-emerald-600 dark:text-emerald-400 border border-emerald-500/20">Tier 1</span>
                      </div>
                      <div class="text-[10px] text-muted-foreground truncate mt-0.5">High cumulative saves across all creator boards</div>
                    </div>
                  </div>
                  <div class="flex items-center gap-1.5 shrink-0">
                    <label for="setting-min-saves" class="text-xs font-semibold text-muted-foreground whitespace-nowrap">Min:</label>
                    <div class="flex items-center rounded-xl border border-border bg-background px-2 py-1 shadow-2xs focus-within:border-emerald-500 focus-within:ring-2 focus-within:ring-emerald-500/20">
                      <input id="setting-min-saves" type="number" min="0" value="25" class="w-14 bg-transparent text-xs font-bold text-foreground text-center font-mono focus:outline-none" />
                      <span class="text-[10px] font-semibold text-muted-foreground pl-1">saves</span>
                    </div>
                  </div>
                </div>

                <!-- Row 2: High Virality (Min Repins) -->
                <div class="flex items-center justify-between gap-3 p-3 rounded-xl border border-blue-500/25 bg-blue-500/5 hover:border-blue-500/45 transition-all group">
                  <div class="flex items-center gap-3 min-w-0">
                    <div class="flex h-8 w-8 shrink-0 items-center justify-center rounded-xl bg-blue-500/15 text-blue-600 dark:text-blue-400 text-sm font-bold border border-blue-500/25 shadow-2xs group-hover:scale-105 transition-transform">
                      🔁
                    </div>
                    <div class="min-w-0">
                      <div class="flex items-center gap-2">
                        <span class="text-xs font-bold text-foreground truncate">High Virality (Repins)</span>
                        <span class="text-[9px] font-bold uppercase tracking-wider px-1.5 py-0.2 rounded bg-blue-500/10 text-blue-600 dark:text-blue-400 border border-blue-500/20">Tier 2</span>
                      </div>
                      <div class="text-[10px] text-muted-foreground truncate mt-0.5">Viral repin distribution across Pinterest networks</div>
                    </div>
                  </div>
                  <div class="flex items-center gap-1.5 shrink-0">
                    <label for="setting-min-repins" class="text-xs font-semibold text-muted-foreground whitespace-nowrap">Min:</label>
                    <div class="flex items-center rounded-xl border border-border bg-background px-2 py-1 shadow-2xs focus-within:border-blue-500 focus-within:ring-2 focus-within:ring-blue-500/20">
                      <input id="setting-min-repins" type="number" min="0" value="25" class="w-14 bg-transparent text-xs font-bold text-foreground text-center font-mono focus:outline-none" />
                      <span class="text-[10px] font-semibold text-muted-foreground pl-1">repins</span>
                    </div>
                  </div>
                </div>

                <!-- Row 3: Fast Risers (Velocity Rule) -->
                <div class="flex items-center justify-between gap-3 p-3 rounded-xl border border-amber-500/25 bg-amber-500/5 hover:border-amber-500/45 transition-all group">
                  <div class="flex items-center gap-3 min-w-0">
                    <div class="flex h-8 w-8 shrink-0 items-center justify-center rounded-xl bg-amber-500/15 text-amber-600 dark:text-amber-400 text-sm font-bold border border-amber-500/25 shadow-2xs group-hover:scale-105 transition-transform">
                      🚀
                    </div>
                    <div class="min-w-0">
                      <div class="flex items-center gap-2">
                        <span class="text-xs font-bold text-foreground truncate">Fast Risers (Velocity)</span>
                        <span class="text-[9px] font-bold uppercase tracking-wider px-1.5 py-0.2 rounded bg-amber-500/10 text-amber-600 dark:text-amber-400 border border-amber-500/20">Tier 3</span>
                      </div>
                      <div class="text-[10px] text-muted-foreground truncate mt-0.5">Fresh pins showing rapid early engagement growth</div>
                    </div>
                  </div>
                  <div class="flex items-center gap-1.5 shrink-0">
                    <div class="flex items-center rounded-xl border border-border bg-background px-2 py-1 shadow-2xs focus-within:border-amber-500 focus-within:ring-2 focus-within:ring-amber-500/20 gap-1">
                      <span class="text-[11px] text-muted-foreground font-mono">&lt;</span>
                      <input id="setting-rising-age" type="number" min="1" max="90" value="14" class="w-7 bg-transparent text-xs font-bold text-foreground text-center font-mono focus:outline-none" title="Max age in days" />
                      <span class="text-[10px] text-muted-foreground">d</span>
                      <span class="text-[10px] text-muted-foreground font-bold">&amp; &ge;</span>
                      <input id="setting-rising-saves" type="number" min="1" value="20" class="w-8 bg-transparent text-xs font-bold text-foreground text-center font-mono focus:outline-none" title="Min saves within age limit" />
                      <span class="text-[10px] font-semibold text-muted-foreground">saves</span>
                    </div>
                  </div>
                </div>

              </div>
            </div>

            <!-- Dynamic Logic Explanation Bar (100% English) -->
            <div id="pinarchive-logic-bar" class="rounded-xl border border-primary/20 bg-gradient-to-r from-primary/10 via-primary/5 to-transparent p-3 sm:px-4 flex items-center justify-between gap-3 shadow-2xs">
              <div class="flex items-center gap-2.5 min-w-0">
                <span class="relative flex h-2.5 w-2.5 shrink-0">
                  <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-primary opacity-75"></span>
                  <span class="relative inline-flex rounded-full h-2.5 w-2.5 bg-primary"></span>
                </span>
                <p class="text-[11px] text-foreground font-medium leading-snug">
                  <strong class="text-primary font-bold">OR Condition Active:</strong> Any pin matching <span class="underline decoration-primary/50 underline-offset-2 font-bold">ANY single rule above</span> will be automatically preserved, ranked, and synced to Supabase &amp; Google Sheets.
                </p>
              </div>
              <span class="hidden md:inline-flex items-center gap-1 text-[10px] font-bold uppercase tracking-wider text-muted-foreground whitespace-nowrap bg-background/80 px-2 py-0.5 rounded-md border border-border/60">
                Auto-Evaluated
              </span>
            </div>
          </div>

          <!-- RIGHT CARD (col-span-5): Automation Routines with Pixel-Perfect Standard Switches -->
          <div class="lg:col-span-5 rounded-2xl border border-border/80 bg-gradient-to-b from-card via-card to-background/50 p-4.5 sm:p-5 shadow-xs flex flex-col justify-between space-y-4">
            <div>
              <div class="flex items-center justify-between pb-3 border-b border-border/60 mb-3.5">
                <div class="flex items-center gap-2">
                  <span class="text-xs font-bold text-foreground">⚡ Automation Routines</span>
                  <span class="text-[10px] text-muted-foreground font-medium">Pipeline &amp; sync schedules</span>
                </div>
              </div>

              <!-- 3 Stacked Routine Rows -->
              <div class="space-y-3">
                
                <!-- Row 1: Monthly Audit Sweep -->
                <div class="flex items-center justify-between gap-3 p-3 rounded-xl border border-border/70 bg-background/60 hover:border-primary/30 transition-all group">
                  <div class="flex items-center gap-3 min-w-0">
                    <div class="flex h-8 w-8 shrink-0 items-center justify-center rounded-xl bg-muted text-foreground text-sm border border-border/60 shadow-2xs group-hover:scale-105 transition-transform">
                      🗓️
                    </div>
                    <div class="min-w-0">
                      <div class="text-xs font-bold text-foreground truncate">Monthly Audit Sweep</div>
                      <div class="text-[10px] text-muted-foreground truncate mt-0.5">Day-1 02:00 UTC · Full deep account scan</div>
                    </div>
                  </div>
                  <div class="flex items-center gap-2.5 shrink-0">
                    <span id="setting-audit-sweep-label" class="text-xs font-bold text-emerald-500">Active</span>
                    <label class="relative inline-flex items-center cursor-pointer shrink-0">
                      <input id="setting-audit-sweep-enabled" type="checkbox" checked class="sr-only peer" />
                      <div class="w-11 h-6 bg-zinc-300 dark:bg-zinc-700 peer-focus:outline-none rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:rounded-full after:h-5 after:w-5 after:transition-all after:shadow-sm peer-checked:bg-emerald-500"></div>
                    </label>
                  </div>
                </div>

                <!-- Row 2: Daily Pipeline Cron -->
                <div class="flex items-center justify-between gap-3 p-3 rounded-xl border border-border/70 bg-background/60 hover:border-primary/30 transition-all group">
                  <div class="flex items-center gap-3 min-w-0">
                    <div class="flex h-8 w-8 shrink-0 items-center justify-center rounded-xl bg-muted text-foreground text-sm border border-border/60 shadow-2xs group-hover:scale-105 transition-transform">
                      ⏰
                    </div>
                    <div class="min-w-0">
                      <div class="text-xs font-bold text-foreground truncate">Daily Pipeline Cron</div>
                      <div class="text-[10px] text-muted-foreground truncate mt-0.5">Daily @ 07:00 UTC · FastCron + Matrix</div>
                    </div>
                  </div>
                  <div class="flex items-center gap-2.5 shrink-0">
                    <span id="setting-github-schedule-label" class="text-xs font-bold text-emerald-500">Active</span>
                    <label class="relative inline-flex items-center cursor-pointer shrink-0">
                      <input id="setting-github-schedule" type="checkbox" checked class="sr-only peer" />
                      <div class="w-11 h-6 bg-zinc-300 dark:bg-zinc-700 peer-focus:outline-none rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:rounded-full after:h-5 after:w-5 after:transition-all after:shadow-sm peer-checked:bg-emerald-500"></div>
                    </label>
                  </div>
                </div>

                <!-- Row 3: Google Sheet Sync -->
                <div class="flex items-center justify-between gap-3 p-3 rounded-xl border border-border/70 bg-background/60 hover:border-primary/30 transition-all group">
                  <div class="flex items-center gap-3 min-w-0">
                    <div class="flex h-8 w-8 shrink-0 items-center justify-center rounded-xl bg-muted text-foreground text-sm border border-border/60 shadow-2xs group-hover:scale-105 transition-transform">
                      📑
                    </div>
                    <div class="min-w-0">
                      <div class="text-xs font-bold text-foreground truncate">Google Sheet Sync</div>
                      <div class="text-[10px] text-muted-foreground truncate mt-0.5">Sheets ⇄ Supabase DB 2-way sync</div>
                    </div>
                  </div>
                  <div class="flex items-center gap-2.5 shrink-0">
                    <span id="setting-daily-sheet-sync-label" class="text-xs font-bold text-muted-foreground">Off</span>
                    <label class="relative inline-flex items-center cursor-pointer shrink-0">
                      <input id="setting-daily-sheet-sync" type="checkbox" class="sr-only peer" />
                      <div class="w-11 h-6 bg-zinc-300 dark:bg-zinc-700 peer-focus:outline-none rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:rounded-full after:h-5 after:w-5 after:transition-all after:shadow-sm peer-checked:bg-emerald-500"></div>
                    </label>
                  </div>
                </div>

              </div>
            </div>

            <!-- Transparency status in right card footer -->
            <div class="rounded-xl border border-border/60 bg-muted/30 p-2.5 sm:px-3 flex items-center justify-between text-[11px] text-muted-foreground shadow-2xs">
              <div class="flex items-center gap-2">
                <span id="pipeline-gh-status-dot" class="h-2 w-2 rounded-full bg-primary"></span>
                <span id="pipeline-gh-status-text">Daily @ 07:00 UTC</span>
              </div>
              <span id="transparency-cost-estimate" class="font-medium">Estimating…</span>
            </div>
          </div>

        </div>

        <!-- ═══ 3. Collapsible Advanced Scraper Limits ═══ -->
        <details class="group rounded-xl border border-border/60 bg-muted/20 overflow-hidden transition-all">
          <summary class="flex items-center justify-between p-3.5 text-xs font-bold text-muted-foreground hover:text-foreground cursor-pointer select-none bg-muted/30">
            <div class="flex items-center gap-2">
              <span>⚙️ Advanced Scraper Limits &amp; Engine Controls</span>
              <span class="text-[10px] font-normal text-muted-foreground">(Batch sizing, Early-Stop depth, and refresh caps)</span>
            </div>
            <svg class="h-4 w-4 transition-transform group-open:rotate-180 text-muted-foreground" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="6 9 12 15 18 9"/></svg>
          </summary>

          <div class="p-4 space-y-4 bg-background/50 border-t border-border/60">
            <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-6 gap-3">
              <!-- Master Switch -->
              <div class="rounded-xl border border-border/80 bg-card p-3 flex flex-col justify-between shadow-2xs">
                <div class="flex items-center justify-between">
                  <label for="setting-ingest-enabled" class="text-xs font-bold text-foreground">Master Ingest</label>
                  <span id="setting-ingest-label" class="text-[11px] font-bold text-emerald-500">Active</span>
                </div>
                <div class="mt-2 flex items-center justify-between">
                  <span class="text-[10px] text-muted-foreground">Accept runs</span>
                  <label class="relative inline-flex items-center cursor-pointer shrink-0">
                    <input id="setting-ingest-enabled" type="checkbox" checked class="sr-only peer" />
                    <div class="w-9 h-5 bg-zinc-300 dark:bg-zinc-700 peer-focus:outline-none rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:rounded-full after:h-4 after:w-4 after:transition-all after:shadow-xs peer-checked:bg-emerald-500"></div>
                  </label>
                </div>
              </div>

              <!-- Paused Account Policy -->
              <div class="rounded-xl border border-border/80 bg-card p-3 flex flex-col justify-between shadow-2xs">
                <label for="setting-paused-policy" class="text-xs font-bold text-foreground">Paused Policy</label>
                <select id="setting-paused-policy" class="mt-2 w-full rounded-lg border border-border bg-background px-2 py-1 text-xs font-medium text-foreground focus:border-primary">
                  <option value="reject">Reject (Disabled)</option>
                  <option value="accept">Allow Silently</option>
                </select>
              </div>

              <!-- Max Batch Pins -->
              <div class="rounded-xl border border-border/80 bg-card p-3 flex flex-col justify-between shadow-2xs">
                <label for="setting-max-batch" class="text-xs font-bold text-foreground">Max Batch Pins</label>
                <div class="mt-2 flex items-center gap-1.5">
                  <input id="setting-max-batch" type="number" min="10" max="5000" value="500" class="w-full rounded-lg border border-border bg-background px-2 py-1 text-xs font-bold text-foreground font-mono" />
                  <span class="text-[10px] text-muted-foreground">pins</span>
                </div>
              </div>

              <!-- Discovery Max Pages -->
              <div class="rounded-xl border border-border/80 bg-card p-3 flex flex-col justify-between shadow-2xs">
                <label for="setting-discovery-max-pages" class="text-xs font-bold text-foreground">Discovery Max Pages</label>
                <div class="mt-2 flex items-center gap-1.5">
                  <input id="setting-discovery-max-pages" type="number" min="1" max="500" value="50" class="w-full rounded-lg border border-border bg-background px-2 py-1 text-xs font-bold text-foreground font-mono" />
                  <span class="text-[10px] text-muted-foreground">pages</span>
                </div>
              </div>

              <!-- Discovery Stop (Early-Stop) -->
              <div class="rounded-xl border border-border/80 bg-card p-3 flex flex-col justify-between shadow-2xs">
                <label for="setting-discovery-stop-pages" class="text-xs font-bold text-foreground">Early-Stop Pages</label>
                <div class="mt-2 flex items-center gap-1.5">
                  <input id="setting-discovery-stop-pages" type="number" min="1" max="10" value="3" class="w-full rounded-lg border border-border bg-background px-2 py-1 text-xs font-bold text-foreground font-mono" />
                  <span class="text-[10px] text-muted-foreground">pages</span>
                </div>
              </div>

              <!-- Refresh Max Pins -->
              <div class="rounded-xl border border-border/80 bg-card p-3 flex flex-col justify-between shadow-2xs">
                <label for="setting-refresh-max-pins" class="text-xs font-bold text-foreground">Refresh Max Pins</label>
                <div class="mt-2 flex items-center gap-1.5">
                  <input id="setting-refresh-max-pins" type="number" min="0" max="10000" value="0" class="w-full rounded-lg border border-border bg-background px-2 py-1 text-xs font-bold text-foreground font-mono" />
                  <span class="text-[10px] text-muted-foreground">(0=all)</span>
                </div>
              </div>
            </div>

            <!-- Refresh Min Saves -->
            <div class="hidden">
              <input id="setting-refresh-min-saves" type="number" value="0" />
            </div>
          </div>
        </details>

      </div>
    </section>

    <!-- ═══ View A: Accounts Table with 2-Tier Controls ═══ -->
    <section class="mb-8 rounded-2xl border border-border bg-card shadow-xs relative" aria-labelledby="accounts-section-heading">
      
      <!-- Tier 1: View Navigation & Display Controls -->
      <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3 border-b border-border px-5 py-3 bg-muted/20 rounded-t-2xl">
        
        <!-- Left: Status Tabs -->
        <div class="flex items-center gap-1 bg-muted/70 p-1 rounded-xl border border-border/60 overflow-x-auto">
          <button data-status-tab="all" class="pa-status-tab-btn rounded-lg px-3 py-1.5 text-xs font-semibold bg-background text-foreground shadow-2xs transition-all whitespace-nowrap">All Accounts</button>
          <button data-status-tab="active" class="pa-status-tab-btn rounded-lg px-3 py-1.5 text-xs font-semibold text-muted-foreground hover:text-foreground transition-all whitespace-nowrap">🟢 Active</button>
          <button data-status-tab="paused" class="pa-status-tab-btn rounded-lg px-3 py-1.5 text-xs font-semibold text-muted-foreground hover:text-foreground transition-all whitespace-nowrap">⏸ Paused</button>
        </div>

        <!-- Right: Display Controls Group -->
        <div class="flex items-center gap-2.5 flex-wrap self-start sm:self-auto">
          <!-- Segmented Display Toolbar -->
          <div class="flex items-center rounded-xl border border-border bg-background p-0.5 shadow-2xs">
            <!-- Number Format Toggle -->
            <button id="pa-num-format-toggle-btn" class="inline-flex items-center gap-1 rounded-lg px-2.5 py-1 text-xs font-medium text-muted-foreground hover:text-foreground hover:bg-muted transition-all" title="Toggle Compact / Full number format">
              <span class="text-[10px] font-mono">#</span>
              <span id="pa-num-format-label">Full</span>
            </button>

            <div class="h-3.5 w-px bg-border my-auto"></div>

            <!-- Density Toggle -->
            <button id="pa-density-toggle-btn" class="inline-flex items-center gap-1 rounded-lg px-2.5 py-1 text-xs font-medium text-muted-foreground hover:text-foreground hover:bg-muted transition-all" title="Toggle Table Density (Comfortable / Compact)">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="3" y1="6" x2="21" y2="6"/><line x1="3" y1="12" x2="21" y2="12"/><line x1="3" y1="18" x2="21" y2="18"/></svg>
              <span id="pa-density-label">Comfortable</span>
            </button>

            <div class="h-3.5 w-px bg-border my-auto"></div>

            <!-- Columns Dropdown -->
            <div class="relative" id="pa-col-visibility-wrapper">
              <button id="pa-col-visibility-btn" type="button" class="inline-flex items-center gap-1 rounded-lg px-2.5 py-1 text-xs font-medium text-muted-foreground hover:text-foreground hover:bg-muted transition-all">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5 text-primary" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
                <span>Columns</span>
                <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3 text-muted-foreground" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
              </button>
              <div id="pa-col-visibility-menu" class="hidden absolute right-0 top-full mt-1.5 z-50 w-56 rounded-2xl border border-border bg-card shadow-xl p-2.5 space-y-1"></div>
            </div>
          </div>

          <!-- Counter Pill -->
          <span class="inline-flex items-center rounded-xl bg-primary/10 px-3 py-1 text-xs font-bold text-primary border border-primary/20" id="accounts-strip-count">
            0 Accounts
          </span>
        </div>
      </div>

      <!-- Tier 2: Unified Search & Filter Bar -->
      <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-2.5 border-b border-border px-5 py-2.5 bg-background">
        <!-- Search Input -->
        <div class="relative flex-1 max-w-md">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5 absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground pointer-events-none" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
          <input
            id="account-search-input"
            type="text"
            placeholder="Search creator handle, status…"
            class="w-full h-8.5 rounded-xl border border-border bg-muted/20 py-1.5 pl-8 pr-12 text-xs text-foreground placeholder:text-muted-foreground focus:bg-background focus:border-primary focus:outline-none focus:ring-1 focus:ring-primary transition-all shadow-2xs"
          />
          <button id="pa-search-clear-btn" class="hidden absolute right-6 top-1/2 -translate-y-1/2 text-muted-foreground hover:text-foreground text-xs p-0.5" title="Clear search">✕</button>
          <kbd class="absolute right-2 top-1/2 -translate-y-1/2 rounded border border-border bg-card px-1 text-[9px] font-bold text-muted-foreground pointer-events-none">/</kbd>
        </div>

        <!-- Interval Filter Dropdown -->
        <div class="flex items-center gap-2">
          <select id="pa-interval-filter" class="h-8.5 rounded-xl border border-border bg-muted/20 hover:bg-background px-3 py-1 text-xs font-medium text-foreground focus:border-primary focus:outline-none transition-colors shadow-2xs">
            <option value="all">⚡ All Intervals</option>
            <option value="1">1 Day</option>
            <option value="3">3 Days</option>
            <option value="7">7 Days</option>
            <option value="14">14 Days</option>
          </select>
        </div>
      </div>

      <!-- Account Selection Action Bar -->
      <div id="accounts-action-bar" class="hidden items-center justify-between gap-3 border-b border-border bg-primary/10 px-5 py-2.5 flex-wrap animate-in fade-in duration-150">
        <div class="flex items-center gap-3">
          <span id="accounts-selected-label" class="text-xs font-bold text-primary">0 selected</span>
        </div>
        
        <div class="flex items-center gap-2 flex-wrap">
          <button
            id="refresh-selected-accounts-btn"
            type="button"
            class="h-8 inline-flex items-center gap-1.5 rounded-xl bg-primary px-3 text-xs font-bold text-primary-foreground shadow-xs hover:bg-primary/90 transition-all cursor-pointer"
            title="⚡ Fast Metrics Sync: Refreshes engagement metrics (saves, repins, reach) for selected accounts (~1-2 min)"
          >
            <svg id="refresh-selected-icon" xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 12a9 9 0 1 1-2.64-6.36"/><polyline points="21 3 21 9 15 9"/></svg>
            <span id="refresh-selected-btn-text">⚡ Fast Refresh</span>
          </button>

          <button
            id="gas-bulk-run-btn"
            type="button"
            class="h-8 inline-flex items-center gap-1.5 rounded-xl border border-border bg-background hover:bg-muted px-2.5 text-xs font-semibold text-foreground shadow-2xs transition-colors cursor-pointer"
            title="🧠 Full Discovery Pipeline: Searches Pinterest for newly published pins and archives them"
          >
            <span>🧠 Discover Pins</span>
          </button>

          <!-- Bulk Interval Dropdown -->
          <div class="flex items-center gap-1.5 bg-background border border-border rounded-xl px-2 py-0.5 shadow-2xs">
            <span class="text-[11px] font-semibold text-muted-foreground whitespace-nowrap">⏱ Set Interval:</span>
            <select
              id="bulk-interval-select"
              class="bg-transparent text-xs font-bold text-foreground py-1 px-1 focus:outline-none cursor-pointer"
              title="Change scrape interval for selected accounts"
            >
              <option value="" disabled selected>Select…</option>
              <option value="1">1 day (Daily) - Default</option>
              <option value="2">2 days</option>
              <option value="3">3 days</option>
              <option value="5">5 days</option>
              <option value="7">7 days (Weekly)</option>
              <option value="14">14 days (Bi-weekly)</option>
              <option value="30">30 days (Monthly)</option>
            </select>
          </div>

          <button
            id="send-selected-to-competitors-btn"
            type="button"
            class="h-8 inline-flex items-center gap-1.5 rounded-xl border border-emerald-500/30 bg-emerald-500/10 hover:bg-emerald-500/20 text-emerald-600 dark:text-emerald-400 px-3 text-xs font-bold shadow-2xs transition-colors cursor-pointer"
            title="Track Selected in Competitor Intelligence"
          >
            <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="22" y1="12" x2="18" y2="12"/><line x1="6" y1="12" x2="2" y2="12"/><line x1="12" y1="6" x2="12" y2="2"/><line x1="12" y1="22" x2="12" y2="18"/></svg>
            <span>🎯 Send to Competitors</span>
          </button>

          <button
            id="gas-bulk-pause-btn"
            type="button"
            class="h-8 inline-flex items-center gap-1.5 rounded-xl border border-border bg-background hover:bg-muted px-2.5 text-xs font-semibold text-foreground shadow-2xs transition-colors cursor-pointer"
            title="Pause Ingestion: Temporarily pauses automated scraping for selected accounts"
          >
            <span>⏸ Pause Ingest</span>
          </button>

          <button
            id="gas-bulk-resume-btn"
            type="button"
            class="h-8 inline-flex items-center gap-1.5 rounded-xl border border-border bg-background hover:bg-muted px-2.5 text-xs font-semibold text-foreground shadow-2xs transition-colors cursor-pointer"
            title="Resume Ingestion: Re-enables automated scraping for selected accounts"
          >
            <span>▶ Resume Ingest</span>
          </button>

          <button
            id="delete-selected-accounts-btn"
            type="button"
            class="h-8 inline-flex items-center gap-1.5 rounded-xl bg-rose-600 hover:bg-rose-500 text-white px-3 text-xs font-bold shadow-xs transition-colors cursor-pointer"
            title="Delete Selected Accounts: Permanently removes selected accounts and their archived records"
          >
            <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/><line x1="10" y1="11" x2="10" y2="17"/><line x1="14" y1="11" x2="14" y2="17"/></svg>
            <span id="delete-accounts-btn-text">Delete Selected</span>
          </button>

          <button
            id="clear-account-selection-btn"
            type="button"
            class="h-8 rounded-xl px-2.5 text-xs font-semibold text-muted-foreground hover:text-foreground hover:bg-muted transition-colors cursor-pointer"
          >
            Cancel
          </button>
        </div>
      </div>

      <!-- Data Table Container -->
      <div class="overflow-x-auto min-w-full">
        <table class="w-full text-left text-xs border-collapse" id="pinarchive-accounts-table">
          <thead class="sticky top-0 z-10 border-b border-border bg-card/95 backdrop-blur-sm text-foreground font-semibold"></thead>
          <tbody id="accounts-table-body" class="divide-y divide-border/70 bg-card">
            <tr>
              <td colspan="10" class="p-8 text-center text-xs text-muted-foreground animate-pulse">Loading tracked accounts…</td>
            </tr>
          </tbody>
        </table>
      </div>
    </section>

    <!-- ═══ Add Creator Account Modal ═══ -->
    <div id="add-creator-modal" class="hidden fixed inset-0 z-50 flex items-center justify-center bg-black/60 backdrop-blur-xs p-4">
      <div class="relative w-full max-w-md rounded-2xl border border-border bg-card p-6 shadow-2xl space-y-4">
        <div class="flex items-center justify-between border-b border-border pb-3">
          <div class="flex items-center gap-3">
            <span class="flex h-10 w-10 items-center justify-center rounded-xl bg-primary/10 text-primary border border-primary/20">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><line x1="19" y1="8" x2="19" y2="14"/><line x1="22" y1="11" x2="16" y2="11"/></svg>
            </span>
            <div>
              <h3 class="text-base font-bold text-foreground">Track Pinterest Creator</h3>
              <p class="text-xs text-muted-foreground">Add account to PinArchive Ingestion Pipeline</p>
            </div>
          </div>
          <button id="close-add-creator-modal-btn" type="button" class="text-muted-foreground hover:text-foreground text-sm p-1 cursor-pointer">✕</button>
        </div>

        <form id="add-creator-form" class="space-y-4">
          <div class="space-y-1.5">
            <label for="new-creator-username" class="block text-xs font-bold text-foreground">
              Pinterest Handle or Profile URL <span class="text-rose-500">*</span>
            </label>
            <div class="relative">
              <span class="absolute left-3 top-1/2 -translate-y-1/2 text-muted-foreground font-mono text-xs">@</span>
              <input
                id="new-creator-username"
                type="text"
                required
                placeholder="pinterest_creator or url"
                class="w-full rounded-xl border border-border bg-background py-2 pl-7 pr-3 text-xs text-foreground placeholder:text-muted-foreground focus:border-primary focus:outline-none focus:ring-1 focus:ring-primary shadow-2xs"
              />
            </div>
            <p class="text-[11px] text-muted-foreground">e.g. <code>cicisafriajit</code> or <code>https://www.pinterest.com/cicisafriajit/</code></p>
          </div>

          <div class="space-y-1.5">
            <label for="new-creator-interval" class="block text-xs font-bold text-foreground">
              Discovery / Ingest Interval
            </label>
            <select
              id="new-creator-interval"
              class="w-full rounded-xl border border-border bg-background p-2 text-xs font-semibold text-foreground focus:border-primary focus:outline-none focus:ring-1 focus:ring-primary shadow-2xs cursor-pointer"
            >
              <option value="1" selected>1 day (Daily) - Recommended</option>
              <option value="2">2 days</option>
              <option value="3">3 days</option>
              <option value="5">5 days</option>
              <option value="7">7 days (Weekly)</option>
              <option value="14">14 days (Bi-weekly)</option>
              <option value="30">30 days (Monthly)</option>
            </select>
          </div>

          <!-- Cross-Module Sync Checkbox -->
          <!-- Cross-Module Sync & Immediate Scraping Checkbox -->
          <div class="rounded-xl bg-muted/40 border border-border/80 p-3 flex items-start gap-2.5">
            <input
              id="sync-to-competitors-checkbox"
              type="checkbox"
              checked
              class="mt-0.5 rounded accent-primary h-4 w-4 cursor-pointer"
            />
            <label for="sync-to-competitors-checkbox" class="text-xs text-foreground cursor-pointer select-none">
              <span class="font-bold block">🎯 Also track & scrape in Competitor Intelligence</span>
              <span class="text-[11px] text-muted-foreground block">Adds creator to Competitors DB and triggers immediate profile metrics scraping.</span>
            </label>
          </div>

          <!-- Run Discover Pins Immediately Checkbox -->
          <div class="rounded-xl bg-muted/40 border border-border/80 p-3 flex items-start gap-2.5">
            <input
              id="run-discovery-now-checkbox"
              type="checkbox"
              checked
              class="mt-0.5 rounded accent-primary h-4 w-4 cursor-pointer"
            />
            <label for="run-discovery-now-checkbox" class="text-xs text-foreground cursor-pointer select-none">
              <span class="font-bold block">🧠 Run Discover Pins immediately</span>
              <span class="text-[11px] text-muted-foreground block">Dispatches the PinArchive discovery pipeline to fetch and archive viral candidate pins right away.</span>
            </label>
          </div>

          <div id="add-creator-error" class="hidden text-xs text-rose-500 bg-rose-500/10 border border-rose-500/20 p-2.5 rounded-xl font-medium"></div>

          <div class="flex items-center justify-end gap-2 pt-3 border-t border-border">
            <button
              id="cancel-add-creator-modal-btn"
              type="button"
              class="rounded-xl border border-border bg-background px-3.5 py-2 text-xs font-semibold text-muted-foreground hover:text-foreground transition-colors cursor-pointer"
            >
              Cancel
            </button>
            <button
              id="submit-add-creator-btn"
              type="submit"
              class="inline-flex items-center gap-1.5 rounded-xl bg-primary hover:bg-primary/90 text-primary-foreground px-4 py-2 text-xs font-bold shadow-sm transition-all cursor-pointer"
            >
              <span>+ Track Creator</span>
            </button>
          </div>
        </form>
      </div>
    </div>

    <!-- ═══ Delete Accounts Confirmation Modal ═══ -->
    <div id="delete-accounts-modal" class="hidden fixed inset-0 z-50 flex items-center justify-center bg-black/60 backdrop-blur-xs p-4">
      <div class="relative w-full max-w-md rounded-2xl border border-rose-500/40 bg-card p-6 shadow-2xl space-y-4">
        <div class="flex items-center gap-3">
          <span class="flex h-10 w-10 items-center justify-center rounded-xl bg-rose-500/10 text-rose-500 border border-rose-500/20">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M10.29 3.86 1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"/><line x1="12" y1="9" x2="12" y2="13"/><line x1="12" y1="17" x2="12.01" y2="17"/></svg>
          </span>
          <div>
            <h3 class="text-base font-bold text-foreground">Confirm Account Deletion</h3>
            <p class="text-xs text-muted-foreground">Permanent database purge.</p>
          </div>
        </div>

        <div class="rounded-xl bg-rose-500/5 border border-rose-500/20 p-3.5 text-xs text-muted-foreground space-y-2.5">
          <p class="text-rose-500 font-bold">⚠️ Warning: Database Deletion is Permanent</p>
          <p><strong class="text-foreground">Permanent Purge:</strong> DB deletion is permanent. GH discovery will NOT re-create the account (source of truth is the DB, not GAS).</p>
        </div>

        <div class="space-y-2">
          <label for="delete-confirm-input" class="block text-xs font-semibold text-foreground">
            Type <span class="font-mono text-rose-500 font-bold">DELETE</span> to confirm:
          </label>
          <input
            id="delete-confirm-input"
            type="text"
            placeholder="DELETE"
            class="w-full rounded-xl border border-border bg-background p-2.5 text-xs text-foreground focus:border-rose-500 focus:outline-none focus:ring-1 focus:ring-rose-500 font-mono"
          />
        </div>

        <div class="flex items-center justify-end gap-2 pt-2 border-t border-border">
          <button
            id="cancel-delete-modal-btn"
            type="button"
            class="rounded-xl border border-border bg-background px-3.5 py-2 text-xs font-semibold text-muted-foreground hover:text-foreground transition-colors cursor-pointer"
          >
            Cancel
          </button>
          <button
            id="confirm-delete-modal-btn"
            type="button"
            disabled
            class="rounded-xl bg-rose-600 hover:bg-rose-500 disabled:opacity-50 disabled:pointer-events-none text-white px-4 py-2 text-xs font-bold shadow-sm transition-colors cursor-pointer"
          >
            Delete Accounts
          </button>
        </div>
      </div>
    </div>
  </div>

  <!-- ═══ MODAL: Platform Architecture & Automation Guide ═══ -->
  <div
    id="architecture-guide-modal"
    class="fixed inset-0 z-50 hidden min-h-screen overflow-y-auto bg-black/60 backdrop-blur-sm p-4 sm:p-6 flex items-center justify-center"
    aria-modal="true"
    role="dialog"
  >
    <div id="architecture-guide-backdrop" class="fixed inset-0"></div>
    <div class="relative w-full max-w-3xl rounded-2xl border border-border bg-card p-6 sm:p-7 shadow-2xl space-y-5 z-10 my-auto max-h-[90vh] flex flex-col">
      
      <!-- Modal Header -->
      <div class="flex items-center justify-between border-b border-border pb-4 flex-shrink-0">
        <div class="flex items-center gap-3">
          <div class="flex h-10 w-10 items-center justify-center rounded-xl bg-primary/10 text-primary border border-primary/20">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M12 2v20M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/>
            </svg>
          </div>
          <div>
            <h3 class="text-base font-bold text-foreground">PinOrbit Architecture &amp; Automation Guide</h3>
            <p class="text-xs text-muted-foreground">دليل الهندسة المعمارية والأتمتة — Dual-Scheduler &amp; Master Workspace</p>
          </div>
        </div>
        <button
          id="close-architecture-guide-modal-btn"
          type="button"
          class="rounded-lg p-1.5 text-muted-foreground hover:bg-muted hover:text-foreground transition-all cursor-pointer"
        >
          <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
        </button>
      </div>

      <!-- Guide Tabs -->
      <div class="flex items-center gap-1.5 bg-muted/60 p-1 rounded-xl border border-border/60 flex-shrink-0 overflow-x-auto">
        <button
          id="guide-tab-btn-master"
          type="button"
          class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-bold bg-background text-foreground shadow-2xs transition-all cursor-pointer whitespace-nowrap"
        >
          <span>👑 Master Workspace</span>
        </button>
        <button
          id="guide-tab-btn-scheduler"
          type="button"
          class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-bold text-muted-foreground hover:text-foreground transition-all cursor-pointer whitespace-nowrap"
        >
          <span>⚡ Dual-Scheduler Engine</span>
        </button>
        <button
          id="guide-tab-btn-matrix"
          type="button"
          class="inline-flex items-center gap-1.5 px-3 py-1.5 rounded-lg text-xs font-bold text-muted-foreground hover:text-foreground transition-all cursor-pointer whitespace-nowrap"
        >
          <span>🚀 4-Shard Matrix Parallel</span>
        </button>
      </div>

      <!-- Guide Content (Scrollable) -->
      <div class="flex-1 overflow-y-auto space-y-4 pr-1 text-xs text-muted-foreground leading-relaxed">
        
        <!-- Tab 1: Master Workspace -->
        <div id="guide-tab-view-master" class="space-y-3.5">
          <div class="rounded-xl border border-amber-500/20 bg-amber-500/5 p-4 space-y-2">
            <h4 class="font-bold text-foreground flex items-center gap-2 text-sm text-amber-600 dark:text-amber-400">
              <span>👑 ما هي المساحة الرئيسية (Master Workspace)؟</span>
            </h4>
            <p>
              المساحة الرئيسية هي <strong>مركز القيادة الشامل للمنصة</strong>. عند تعيين مساحة عمل كـ Master Workspace:
            </p>
            <ul class="list-disc list-inside space-y-1.5 pl-1">
              <li><strong>⚡ زر Run Now الشامل:</strong> يطلق المزامنة لكل الحسابات في <strong>جميع مساحات العمل دفعة واحدة</strong> بالتوازي عبر الـ 4 خوادم.</li>
              <li><strong>🕒 مهمة FastCron واحدة للمنصة:</strong> يمكنك إنشاء مهمة FastCron في الـ Master Workspace لتقوم بمزامنة وتحديث كافة المساحات في توقيت مخصص تختاره أنت.</li>
              <li><strong>🛑 مفتاح الإيقاف العام (Global Kill-Switch):</strong> تعطيل مجدول GitHub في الـ Master Workspace يوقف المجدول التلقائي للمنصة ككل فوراً.</li>
            </ul>
          </div>

          <div class="rounded-xl border border-border bg-background p-4 space-y-2">
            <h5 class="font-bold text-foreground">كيف تعيّن مساحة كـ Master Workspace؟</h5>
            <p>
              من قائمة اختيار المساحات بالأعلى، اضغط على <strong>Manage Workspaces (⚙️)</strong> ثم اضغط على زر <strong>Set Master 👑</strong> بجانب المساحة المطلوبة.
            </p>
          </div>
        </div>

        <!-- Tab 2: Dual-Scheduler -->
        <div id="guide-tab-view-scheduler" class="hidden space-y-3.5">
          <div class="rounded-xl border border-primary/20 bg-primary/5 p-4 space-y-2">
            <h4 class="font-bold text-foreground flex items-center gap-2 text-sm text-primary">
              <span>⚡ منظومة المجدول المزدوج (Dual-Scheduler Engine)</span>
            </h4>
            <p>
              تمتلك المنصة خيارين للجدولة التلقائية يمكنك التبديل بينهما بكل حرية واستقلالية:
            </p>
          </div>

          <div class="grid grid-cols-1 sm:grid-cols-2 gap-3">
            <div class="rounded-xl border border-border bg-background p-3.5 space-y-1.5">
              <span class="font-bold text-foreground text-xs flex items-center gap-1.5">
                <span class="h-2 w-2 rounded-full bg-primary"></span>
                <span>1. مجدول GitHub Actions (07:00 UTC)</span>
              </span>
              <p class="text-[11px]">
                يعمل تلقائياً يومياً في تمام الساعة 07:00 UTC (08:00 صباحاً بتوقيت المغرب). إذا كنت لا تريد تشغيله في هذا الوقت، يمكنك تعطيله بزر واحد من لوحة الإعدادات.
              </p>
            </div>

            <div class="rounded-xl border border-border bg-background p-3.5 space-y-1.5">
              <span class="font-bold text-foreground text-xs flex items-center gap-1.5">
                <span class="h-2 w-2 rounded-full bg-amber-500"></span>
                <span>2. مجدول FastCron المخصص</span>
              </span>
              <p class="text-[11px]">
                يتيح لك تحديد التوقيت المفضل بدقة (مثلاً 03:00 UTC أو كل 6 ساعات) وإدارته بحرية كاملة عبر FastCron مع حماية التوكنات وتشفيرها.
              </p>
            </div>
          </div>

          <div class="rounded-xl border border-emerald-500/20 bg-emerald-500/5 p-3 text-[11px] text-emerald-700 dark:text-emerald-300">
            💡 <strong>Zero-Resource Early Exit:</strong> عند تعطيل مجدول GitHub لمساحة ما، يتوقف الاسكربت فوراً في ثانية واحدة [SKIP] دون استهلاك أي دقائق مجانية أو الاتصال بـ Pinterest.
          </div>
        </div>

        <!-- Tab 3: Matrix Sharding -->
        <div id="guide-tab-view-matrix" class="hidden space-y-3.5">
          <div class="rounded-xl border border-border bg-background p-4 space-y-2">
            <h4 class="font-bold text-foreground text-sm flex items-center gap-2">
              <span>🚀 التوزيع المصفوفي المتوازي (Matrix Sharding — 4 Shards)</span>
            </h4>
            <p>
              عندما تملك عدداً كبيراً من الحسابات والبيينز، يقوم النظام تلقائياً بتوزيع الحمل:
            </p>
            <div class="grid grid-cols-2 sm:grid-cols-4 gap-2 pt-2 text-center font-mono text-[11px]">
              <div class="p-2.5 rounded-lg border border-border bg-muted/30">
                <span class="font-bold text-foreground block">Shard 0</span>
                <span class="text-[10px] text-muted-foreground">خادم مستقل 1</span>
              </div>
              <div class="p-2.5 rounded-lg border border-border bg-muted/30">
                <span class="font-bold text-foreground block">Shard 1</span>
                <span class="text-[10px] text-muted-foreground">خادم مستقل 2</span>
              </div>
              <div class="p-2.5 rounded-lg border border-border bg-muted/30">
                <span class="font-bold text-foreground block">Shard 2</span>
                <span class="text-[10px] text-muted-foreground">خادم مستقل 3</span>
              </div>
              <div class="p-2.5 rounded-lg border border-border bg-muted/30">
                <span class="font-bold text-foreground block">Shard 3</span>
                <span class="text-[10px] text-muted-foreground">خادم مستقل 4</span>
              </div>
            </div>
            <p class="pt-2">
              ✨ <strong>النتيجة:</strong> معالجة آلاف البيينز في دقائق معدودة، بدون أي تعارض في أقفال قواعد البيانات (Zero Lock Contention)، مع ضبط تلقائي للسرعة ومعدل الطلبات لتفادي الحظر.
            </p>
          </div>
        </div>
      </div>

      <!-- Modal Footer -->
      <div class="flex items-center justify-end border-t border-border pt-3.5 flex-shrink-0">
        <button
          id="done-architecture-guide-modal-btn"
          type="button"
          class="rounded-xl bg-primary text-primary-foreground px-4 py-2 text-xs font-semibold hover:bg-primary/90 transition-all cursor-pointer shadow-xs"
        >
          Got It
        </button>
      </div>
    </div>
  </div>

  <!-- ═══ Tab 2 View: FastCron Pipeline & Automation ═══ -->
  <div id="pa-tab-view-automation" class="hidden space-y-6">
    <PinArchiveCronCard isMasterWorkspace={isMasterWorkspace} />
  </div>
  </div>
</Layout>

<script>
  let rawAccounts: any[] = [];
  const selectedAccounts = new Set<string>();

  // State
  let activeStatusTab: 'all' | 'active' | 'paused' = 'all';
  let accountSearchTerm = '';
  let selectedInterval = 'all';
  let isCompactNumbers = localStorage.getItem('pa_num_fmt') === 'compact';
  let isCompactDensity = localStorage.getItem('pa_density') === 'compact';
  let competitorMap = new Map<string, { id: string; username: string }>();

  async function loadCompetitorUsernames() {
    try {
      const root = document.getElementById('pinarchive-page-root');
      const wsId = root?.getAttribute('data-workspace-id');
      const url = wsId ? `/api/admin/competitors?workspace_id=${encodeURIComponent(wsId)}` : '/api/admin/competitors';
      const res = await fetch(url, { signal: AbortSignal.timeout(8000) });
      if (res.ok) {
        const json = await res.json();
        if (json.competitors && Array.isArray(json.competitors)) {
          competitorMap.clear();
          for (const c of json.competitors) {
            if (c.username) {
              competitorMap.set(String(c.username).toLowerCase().trim(), {
                id: c.id,
                username: c.username,
              });
            }
          }
        }
      }
    } catch (e) {
      console.warn('Failed to load competitor usernames for PinArchive cross-badging:', e);
    }
  }

  function escapeHtml(str?: string | null): string {
    if (!str) return '';
    return String(str)
      .replace(/&/g, '&amp;')
      .replace(/</g, '&lt;')
      .replace(/>/g, '&gt;')
      .replace(/"/g, '&quot;')
      .replace(/'/g, '&#039;');
  }

  function toast(message: string, type: 'success' | 'error' | 'info' = 'success') {
    let root = document.getElementById('toast-root');
    if (!root) {
      root = document.createElement('div');
      root.id = 'toast-root';
      root.setAttribute('aria-live', 'polite');
      root.className = 'fixed bottom-4 right-4 z-[70] flex flex-col gap-2 w-80 max-w-[calc(100vw-2rem)]';
      document.body.appendChild(root);
    }
    const colors = {
      success: 'border-emerald-500/40 text-emerald-600 dark:text-emerald-400',
      error: 'border-rose-500/40 text-rose-600 dark:text-rose-400',
      info: 'border-primary/40 text-primary',
    };
    const icons = { success: '✓', error: '✕', info: 'ℹ' };
    const el = document.createElement('div');
    el.className = `flex items-start gap-2.5 rounded-xl border bg-card p-3 text-xs font-semibold shadow-lg ${colors[type]} transition-all animate-in fade-in slide-in-from-bottom-2`;
    el.innerHTML = `<span class="flex-shrink-0">${icons[type]}</span><span class="flex-1 text-foreground">${escapeHtml(message)}</span>
      <button class="text-muted-foreground hover:text-foreground cursor-pointer" aria-label="Dismiss">✕</button>`;
    el.querySelector('button')?.addEventListener('click', () => el.remove());
    root.appendChild(el);
    setTimeout(() => {
      el.style.opacity = '0';
      el.style.transition = 'opacity .3s';
      setTimeout(() => el.remove(), 300);
    }, 4200);
  }

  // Number formatters
  function fmtNumberFull(n: number | string | null | undefined): string {
    const num = Number(n) || 0;
    return num.toLocaleString('en-US');
  }

  function fmtNumberCompact(n: number | string | null | undefined): string {
    const num = Number(n) || 0;
    return new Intl.NumberFormat('en-US', { notation: 'compact', maximumFractionDigits: 1 }).format(num);
  }

  function fmtMetric(n: number | string | null | undefined): string {
    return isCompactNumbers ? fmtNumberCompact(n) : fmtNumberFull(n);
  }

  function timeAgo(isoString?: string | null): string {
    if (!isoString) return 'never';
    try {
      const diffMs = Date.now() - new Date(isoString).getTime();
      if (isNaN(diffMs) || diffMs < 0) return 'recently';
      const mins = Math.floor(diffMs / 60000);
      if (mins < 60) return `${Math.max(1, mins)}m ago`;
      const hrs = Math.floor(mins / 60);
      if (hrs < 24) return `${hrs}h ago`;
      const days = Math.floor(hrs / 24);
      return `${days}d ago`;
    } catch {
      return 'recently';
    }
  }

  function formatNextRun(isoString?: string | null): string {
    if (!isoString) return 'Schedule pending';
    try {
      const nextDate = new Date(isoString);
      const nextMs = nextDate.getTime();
      if (isNaN(nextMs)) return 'Schedule pending';
      const diffMs = nextMs - Date.now();
      const dateStr = nextDate.toISOString().slice(0, 10);
      if (diffMs <= 0) {
        return `Overdue (${dateStr})`;
      }
      const mins = Math.floor(diffMs / 60000);
      if (mins < 60) return `in ${Math.max(1, mins)}m (${dateStr} UTC)`;
      const hrs = Math.floor(mins / 60);
      if (hrs < 24) return `in ${hrs}h (${dateStr} UTC)`;
      const days = Math.floor(hrs / 24);
      return `in ${days} days (${dateStr} UTC)`;
    } catch {
      return 'Scheduled';
    }
  }

  function formatTooltipNextRun(isoString?: string | null): string {
    if (!isoString) return 'No sync scheduled';
    try {
      const d = new Date(isoString);
      if (isNaN(d.getTime())) return 'No sync scheduled';
      const yyyyMmDd = d.toISOString().slice(0, 10);
      const hh = String(d.getUTCHours()).padStart(2, '0');
      const mm = String(d.getUTCMinutes()).padStart(2, '0');
      return `Next scheduled sync: ${yyyyMmDd} ${hh}:${mm} UTC`;
    } catch {
      return 'No sync scheduled';
    }
  }

  function getAvatarStyle(username: string): { bg: string; initial: string } {
    const palettes = [
      'bg-gradient-to-br from-rose-500 to-pink-600',
      'bg-gradient-to-br from-violet-500 to-indigo-600',
      'bg-gradient-to-br from-emerald-500 to-teal-600',
      'bg-gradient-to-br from-sky-500 to-blue-600',
      'bg-gradient-to-br from-amber-500 to-orange-600',
      'bg-gradient-to-br from-fuchsia-500 to-purple-600',
    ];
    let hash = 0;
    const clean = username.replace(/^@/, '');
    for (let i = 0; i < clean.length; i++) {
      hash = (hash + clean.charCodeAt(i)) % palettes.length;
    }
    const initial = clean.slice(0, 2).toUpperCase() || 'PA';
    return { bg: palettes[hash], initial };
  }

  async function copyToClipboard(text: string, btn?: HTMLElement) {
    try {
      await navigator.clipboard.writeText(text);
      if (btn) {
        const orig = btn.innerHTML;
        btn.innerHTML = '<span class="text-emerald-500 font-bold">✓</span>';
        setTimeout(() => { btn.innerHTML = orig; }, 1200);
      }
      toast(`Copied "${text}" to clipboard!`);
    } catch {
      toast('Failed to copy', 'error');
    }
  }

  // ─── Filter & Search Engine ───────────────────────────────────────────────
  function filterAndRenderAccounts() {
    let filtered = [...rawAccounts];

    // Status filter
    if (activeStatusTab === 'active') {
      filtered = filtered.filter(a => a.status === 'active' && a.ingest_enabled !== false);
    } else if (activeStatusTab === 'paused') {
      filtered = filtered.filter(a => a.status === 'paused' || a.ingest_enabled === false);
    }

    // Interval filter
    if (selectedInterval !== 'all') {
      filtered = filtered.filter(a => String(a.interval_days || 1) === selectedInterval);
    }

    // Search filter
    if (accountSearchTerm) {
      const term = accountSearchTerm.toLowerCase();
      filtered = filtered.filter((a: any) =>
        String(a.username || '').toLowerCase().includes(term) ||
        String(a.status || '').toLowerCase().includes(term)
      );
    }

    renderAccountsStrip(filtered);
  }

  // ─── Column Visibility ───────────────────────────────────────────────────
  interface ColDef {
    key: string;
    label: string;
    defaultVisible: boolean;
    align?: 'left' | 'right' | 'center';
  }

  const COLUMN_DEFS: ColDef[] = [
    { key: 'followers',      label: 'Followers',       defaultVisible: true,  align: 'right' },
    { key: 'archived_pins',  label: 'Archived Pins',   defaultVisible: true,  align: 'right' },
    { key: 'interval_days',  label: 'Sync Interval',   defaultVisible: true,  align: 'left' },
    { key: 'delta_changed',  label: 'Recent Δ',        defaultVisible: true,  align: 'right' },
    { key: 'last_sync',      label: 'Last Sync',       defaultVisible: true,  align: 'left' },
    { key: 'backfill_status',label: 'Backfill Status', defaultVisible: false, align: 'left' },
    { key: 'backfill_cursor',label: 'Backfill Cursor', defaultVisible: false, align: 'left' },
    { key: 'last_result',    label: 'Last Result',     defaultVisible: false, align: 'left' },
  ];

  let colVisible: Record<string, boolean> = (() => {
    try {
      const saved = localStorage.getItem('po_pa_cols_v4');
      if (saved) {
        const parsed = JSON.parse(saved);
        if (parsed && typeof parsed === 'object' && !Array.isArray(parsed)) {
          return {
            followers: true,
            archived_pins: true,
            interval_days: true,
            delta_changed: true,
            last_sync: true,
            backfill_status: false,
            backfill_cursor: false,
            last_result: false,
            ...parsed,
          };
        }
      }
    } catch {}
    return {
      followers: true,
      archived_pins: true,
      interval_days: true,
      delta_changed: true,
      last_sync: true,
      backfill_status: false,
      backfill_cursor: false,
      last_result: false,
    };
  })();

  function buildColVisibilityMenu() {
    const colMenu = document.getElementById('pa-col-visibility-menu');
    if (!colMenu) return;
    colMenu.innerHTML = `
      <div class="flex items-center justify-between pb-2 px-1 border-b border-border/80 text-[11px] font-bold text-muted-foreground">
        <span>Toggle Columns</span>
        <button id="reset-pa-cols-btn" class="text-primary hover:underline text-[11px] font-bold cursor-pointer">Reset</button>
      </div>
      <div class="space-y-1 max-h-72 overflow-y-auto pt-1.5 pr-1">
        ${COLUMN_DEFS.map(
          (c) => `
          <label class="flex items-center gap-2.5 px-2 py-1.5 rounded-lg cursor-pointer hover:bg-muted/70 transition-colors text-xs font-semibold text-foreground select-none">
            <input type="checkbox" class="rounded-md accent-primary h-4 w-4 cursor-pointer" data-pa-col-key="${c.key}" ${colVisible[c.key] ? 'checked' : ''} />
            <span>${c.label}</span>
          </label>`
        ).join('')}
      </div>`;

    document.getElementById('reset-pa-cols-btn')?.addEventListener('click', () => {
      COLUMN_DEFS.forEach(c => { colVisible[c.key] = c.defaultVisible; });
      localStorage.removeItem('po_pa_cols_v4');
      buildColVisibilityMenu();
      renderTableHeader();
      filterAndRenderAccounts();
    });

    colMenu.querySelectorAll<HTMLInputElement>('input[data-pa-col-key]').forEach((cb) => {
      cb.addEventListener('change', () => {
        const key = cb.dataset.paColKey!;
        colVisible[key] = cb.checked;
        localStorage.setItem('po_pa_cols_v4', JSON.stringify(colVisible));
        renderTableHeader();
        filterAndRenderAccounts();
      });
    });
  }

  function setupColVisibilityToggle() {
    const btn = document.getElementById('pa-col-visibility-btn');
    const colMenu = document.getElementById('pa-col-visibility-menu');
    const wrapper = document.getElementById('pa-col-visibility-wrapper');
    if (!btn || !colMenu) return;

    btn.addEventListener('click', (e) => {
      e.stopPropagation();
      colMenu.classList.toggle('hidden');
    });

    document.addEventListener('click', (e) => {
      if (wrapper && !wrapper.contains(e.target as Node)) {
        colMenu.classList.add('hidden');
      }
    });
  }

  function renderTableHeader() {
    const thead = document.querySelector('#pinarchive-accounts-table thead');
    if (!thead) return;

    let headersHtml = `
      <tr>
        <th class="py-3 px-4 w-10 text-center">
          <input
            type="checkbox"
            id="select-all-accounts-cb"
            class="rounded border-border text-primary focus:ring-primary h-3.5 w-3.5 cursor-pointer accent-primary"
            title="Select all accounts"
          />
        </th>
        <th class="py-3 px-4 font-semibold text-foreground">Creator Account</th>
    `;

    if (colVisible.followers) headersHtml += `<th class="py-3 px-4 font-semibold text-right">Followers</th>`;
    if (colVisible.archived_pins) headersHtml += `<th class="py-3 px-4 font-semibold text-right">Archived Pins</th>`;
    if (colVisible.interval_days) headersHtml += `<th class="py-3 px-4 font-semibold">Interval</th>`;
    if (colVisible.delta_changed) headersHtml += `<th class="py-3 px-4 font-semibold text-right">Recent Δ</th>`;
    if (colVisible.last_sync) headersHtml += `<th class="py-3 px-4 font-semibold">Last Sync</th>`;
    if (colVisible.backfill_status) headersHtml += `<th class="py-3 px-4 font-semibold">Backfill</th>`;
    if (colVisible.backfill_cursor) headersHtml += `<th class="py-3 px-4 font-semibold">Cursor</th>`;
    if (colVisible.last_result) headersHtml += `<th class="py-3 px-4 font-semibold">Last Result</th>`;

    headersHtml += `
        <th class="py-3 px-4 font-semibold text-center">Ingest Active</th>
        <th class="py-3 px-4 text-right font-semibold">Actions</th>
      </tr>
    `;

    thead.innerHTML = headersHtml;

    document.getElementById('select-all-accounts-cb')?.addEventListener('change', (e) => {
      const checked = (e.target as HTMLInputElement).checked;
      const inputs = document.querySelectorAll<HTMLInputElement>('input[data-acc-select]');
      inputs.forEach(cb => {
        cb.checked = checked;
        const id = cb.getAttribute('data-acc-select');
        if (id) {
          if (checked) selectedAccounts.add(id);
          else selectedAccounts.delete(id);
        }
      });
      updateSelectedAccountsUI();
    });
  }

  // ─── Last Result Badge Renderer ───────────────────────────────────────────
  function renderLastResultBadges(raw: string | null | undefined): string {
    if (!raw || raw === '—') return '<span class="text-muted-foreground">—</span>';

    const str = String(raw).trim();
    // Tolerant regex matching:
    // New format: pages=5 fetched=247 +3 sheet=131 (app=110, upd=21, unch=116)
    // Old format: pages=5 +3 qual=3 sheet=131
    const m = str.match(/^pages=(\d+)(?:\s+fetched=(\d+))?\s+\+(\d+)(?:\s+qual=\d+)?\s+sheet=(\d+)(?:\s+\(app=(\d+),\s*upd=(\d+)(?:,\s*unch=(\d+))?\))?(.*)$/i);

    if (!m) {
      // Graceful Fallback: render raw text safely
      return `<span class="text-[11px] text-muted-foreground truncate" title="${escapeHtml(str)}">${escapeHtml(str)}</span>`;
    }

    const pages = m[1];
    const fetched = m[2];
    const newPins = Number(m[3]) || 0;
    const sheet = m[4];
    const app = m[5];
    const upd = m[6];
    const unch = m[7];
    const extra = (m[8] || '').trim();

    const isCircuitBroken = str.includes('circuit-broken');
    const isSheetErr = str.includes('sheet_err');

    let tooltip = `Pages: ${pages}`;
    if (fetched) tooltip += ` (${fetched} pins fetched)`;
    tooltip += ` • DB: +${newPins}`;
    tooltip += ` • Sheet: ${sheet}`;
    if (app !== undefined && upd !== undefined) {
      tooltip += ` (New: ${app}, Updated: ${upd}${unch !== undefined ? `, Unchanged: ${unch}` : ''})`;
    }
    if (extra) tooltip += ` • ${extra}`;

    const dbBadgeClass = newPins > 0
      ? 'bg-emerald-500/10 text-emerald-600 dark:text-emerald-400 border-emerald-500/20 font-semibold'
      : 'bg-muted text-muted-foreground border-border/50 font-normal';

    return `
      <div class="inline-flex items-center gap-1 flex-wrap text-[10.5px]" title="${escapeHtml(tooltip)}">
        <span class="inline-flex items-center px-1.5 py-0.2 rounded bg-muted text-muted-foreground border border-border/60 font-mono">
          ${pages}p
        </span>
        <span class="inline-flex items-center px-1.5 py-0.2 rounded border ${dbBadgeClass} font-mono">
          +${newPins} DB
        </span>
        <span class="inline-flex items-center gap-0.5 px-1.5 py-0.2 rounded bg-blue-500/10 text-blue-600 dark:text-blue-400 border border-blue-500/20 font-mono">
          📑 ${sheet}
        </span>
        ${isCircuitBroken ? '<span class="inline-flex items-center px-1 py-0.2 rounded text-[9px] font-bold bg-amber-500/10 text-amber-600 border border-amber-500/20">Breaker</span>' : ''}
        ${isSheetErr ? '<span class="inline-flex items-center px-1 py-0.2 rounded text-[9px] font-bold bg-rose-500/10 text-rose-600 border border-rose-500/20">SheetErr</span>' : ''}
      </div>
    `;
  }

  // ─── Table Body Renderer ──────────────────────────────────────────────────
  function renderAccountsStrip(accounts: any[]) {
    const tbody = document.getElementById('accounts-table-body');
    const stripCount = document.getElementById('accounts-strip-count');
    if (stripCount) stripCount.textContent = `${accounts.length} ${accounts.length === 1 ? 'Account' : 'Accounts'}`;

    if (!tbody) return;

    if (accounts.length === 0) {
      tbody.innerHTML = `
        <tr>
          <td colspan="12" class="p-12 text-center text-xs text-muted-foreground bg-muted/10">
            <div class="flex flex-col items-center gap-2 max-w-sm mx-auto">
              <span class="text-2xl">🔍</span>
              <span class="font-bold text-foreground">${rawAccounts.length === 0 ? 'No accounts tracked in archive yet' : 'No accounts match active filters'}</span>
              <p class="text-[11px] text-muted-foreground">Adjust your search term or status filters to view tracked accounts.</p>
            </div>
          </td>
        </tr>
      `;
      return;
    }

    const padClass = isCompactDensity ? 'py-2 px-4' : 'py-3.5 px-4';
    const avatarSize = isCompactDensity ? 'h-7 w-7 text-[10px]' : 'h-8 w-8 text-[11px]';

    tbody.innerHTML = accounts
      .map((acc: any) => {
        const isIngestActive = acc.ingest_enabled !== false && acc.status === 'active';
        const isActive = isIngestActive;
        const isChecked = selectedAccounts.has(acc.id);
        const { bg: avatarBg, initial } = getAvatarStyle(acc.username || '');
        const isPaused = acc.status === 'paused' || (acc.status === 'active' && acc.ingest_enabled === false);
        const isError = ['cookie_expired', 'error'].includes(acc.status);
        const badgeText = isPaused
          ? 'PAUSED'
          : escapeHtml(String(acc.status || 'unknown').replace(/_/g, ' ').toUpperCase());
        const badgeClass = isIngestActive
          ? 'bg-emerald-500/10 text-emerald-600 dark:text-emerald-400 border border-emerald-500/20'
          : isPaused
          ? 'bg-amber-500/10 text-amber-600 dark:text-amber-400 border border-amber-500/20'
          : isError
          ? 'bg-rose-500/10 text-rose-600 dark:text-rose-400 border border-rose-500/20'
          : 'bg-muted text-muted-foreground border border-muted-foreground/20';
        const changed = Number(acc.changed_last_refresh ?? 0);
        const total = Number(acc.checked_last_refresh ?? (acc.db_pins_count !== undefined ? acc.db_pins_count : (acc.pins_count ?? 0)));
        const archCount = acc.db_pins_count !== undefined ? acc.db_pins_count : (acc.pins_count ?? 0);

        let dynamicCells = '';

        if (colVisible.followers) {
          dynamicCells += `<td class="${padClass} text-right font-semibold text-foreground font-mono tabular-nums">${fmtMetric(acc.follower_count)}</td>`;
        }

        if (colVisible.archived_pins) {
          dynamicCells += `
            <td class="${padClass} text-right font-semibold text-emerald-600 dark:text-emerald-400 font-mono tabular-nums">
              <span title="Total archived pins with verified metrics">${fmtMetric(archCount)}</span>
            </td>`;
        }

        if (colVisible.interval_days) {
          const currentDays = Number(acc.interval_days || 1);
          dynamicCells += `
            <td class="${padClass} font-medium text-foreground">
              <div class="relative inline-flex items-center">
                <select
                  class="interval-select appearance-none cursor-pointer inline-flex items-center gap-1 pl-2 pr-5 py-0.5 rounded-lg bg-muted hover:bg-muted/80 text-[11px] font-bold border border-border/60 transition-colors focus:ring-1 focus:ring-primary focus:outline-none shadow-2xs"
                  data-username="${escapeHtml(acc.username || '')}"
                  data-account-id="${escapeHtml(acc.id || '')}"
                  data-prev-days="${currentDays}"
                  title="Click to change discovery interval"
                >
                  <option value="1" ${currentDays === 1 ? 'selected' : ''}>1d</option>
                  <option value="2" ${currentDays === 2 ? 'selected' : ''}>2d</option>
                  <option value="3" ${currentDays === 3 ? 'selected' : ''}>3d</option>
                  <option value="5" ${currentDays === 5 ? 'selected' : ''}>5d</option>
                  <option value="7" ${currentDays === 7 ? 'selected' : ''}>7d</option>
                  <option value="14" ${currentDays === 14 ? 'selected' : ''}>14d</option>
                  <option value="30" ${currentDays === 30 ? 'selected' : ''}>30d</option>
                </select>
                <div class="pointer-events-none absolute right-1.5 flex items-center text-muted-foreground">
                  <svg class="h-2.5 w-2.5" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M5.23 7.21a.75.75 0 011.06.02L10 10.94l3.71-3.71a.75.75 0 111.06 1.06l-4.24 4.25a.75.75 0 01-1.06 0L5.21 8.27a.75.75 0 01.02-1.06z" clip-rule="evenodd"/>
                  </svg>
                </div>
              </div>
            </td>
          `;
        }

        if (colVisible.delta_changed) {
          const deltaBadge = changed > 0
            ? `<span class="inline-flex items-center gap-1 font-bold text-[11px] px-2 py-0.5 rounded-full bg-emerald-500/10 text-emerald-600 dark:text-emerald-400 border border-emerald-500/20" title="${changed} of ${total} pins updated in last refresh">+${fmtMetric(changed)} pins</span>`
            : `<span class="text-[11px] text-muted-foreground" title="No pins changed in last refresh">0 change</span>`;
          dynamicCells += `<td class="${padClass} text-right font-mono tabular-nums">${deltaBadge}</td>`;
        }

        if (colVisible.last_sync) {
          const nextStr = formatNextRun(acc.next_run_at);
          dynamicCells += `
            <td class="${padClass} whitespace-nowrap">
              <div class="text-[11px] font-medium text-foreground">
                ${acc.last_run_at ? timeAgo(acc.last_run_at) : '<span class="text-muted-foreground/60">Never</span>'}
              </div>
              <div class="text-[10px] text-muted-foreground mt-0.5" title="${escapeHtml(formatTooltipNextRun(acc.next_run_at))}">
                <span>Next: ${nextStr}</span>
              </div>
            </td>
          `;
        }

        if (colVisible.backfill_status) {
          const isDone = acc.backfill_status === 'done';
          const isProg = acc.backfill_status === 'in_progress';
          dynamicCells += `
            <td class="${padClass}">
              <span class="text-[10px] font-bold uppercase px-2 py-0.5 rounded-full ${
                isDone
                  ? 'bg-emerald-500/10 text-emerald-600 dark:text-emerald-400 border border-emerald-500/20'
                  : isProg
                  ? 'bg-blue-500/10 text-blue-600 dark:text-blue-400 border border-blue-500/20 animate-pulse'
                  : 'bg-muted text-muted-foreground'
              }">
                ${escapeHtml(acc.backfill_status || 'pending')}
              </span>
            </td>
          `;
        }

        if (colVisible.backfill_cursor) {
          const cur = acc.backfill_cursor ? String(acc.backfill_cursor) : '—';
          const shortCur = cur.length > 12 ? cur.slice(0, 10) + '…' : cur;
          dynamicCells += `<td class="${padClass} font-mono text-[10px] text-muted-foreground" title="${escapeHtml(cur)}">${escapeHtml(shortCur)}</td>`;
        }

        if (colVisible.last_result) {
          dynamicCells += `<td class="${padClass} max-w-[240px] whitespace-nowrap">${renderLastResultBadges(acc.last_result)}</td>`;
        }

        const compInfo = competitorMap.get((acc.username || '').toLowerCase().trim());
        const isCompetitor = Boolean(compInfo);
        const compHref = compInfo?.id ? `/competitors/details?id=${encodeURIComponent(compInfo.id)}` : '/competitors';
        const compBadge = isCompetitor
          ? `<a href="${compHref}" title="View @${escapeHtml(acc.username)} in Competitor Intelligence" class="inline-flex items-center gap-1 px-1.5 py-0.2 rounded-md text-[9.5px] font-bold bg-blue-500/10 text-blue-600 dark:text-blue-400 border border-blue-500/20 hover:bg-blue-500/20 transition-colors shadow-2xs">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-2.5 w-2.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="22" y1="12" x2="18" y2="12"/><line x1="6" y1="12" x2="2" y2="12"/><line x1="12" y1="6" x2="12" y2="2"/><line x1="12" y1="22" x2="12" y2="18"/></svg>
              <span>Comp</span>
            </a>`
          : '';

        return `
          <tr class="${isChecked ? 'bg-primary/5 font-medium' : 'hover:bg-muted/40'} transition-colors group">
            <td class="${padClass} text-center w-10">
              <input
                type="checkbox"
                data-acc-select="${escapeHtml(acc.id)}"
                ${isChecked ? 'checked' : ''}
                class="rounded border-border text-primary focus:ring-primary h-3.5 w-3.5 cursor-pointer accent-primary"
                id="acc-check-${escapeHtml(acc.id)}"
              />
            </td>
            <td class="${padClass}">
              <div class="flex items-center gap-2.5">
                <div class="${avatarSize} rounded-xl ${avatarBg} text-white font-bold flex items-center justify-center shrink-0 shadow-2xs uppercase tracking-tight">
                  ${initial}
                </div>
                <div class="flex flex-col min-w-0">
                  <div class="flex items-center gap-1.5 flex-wrap">
                    <a href="/pinarchive/account/${encodeURIComponent(acc.username)}" class="font-bold text-foreground hover:text-primary transition-colors truncate max-w-[180px]">
                      @${escapeHtml(acc.username)}
                    </a>
                    <button data-pa-copy-handle="${escapeHtml(acc.username)}" class="opacity-40 hover:opacity-100 transition-opacity p-0.5" title="Copy username">
                      <svg xmlns="http://www.w3.org/2000/svg" class="h-2.5 w-2.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>
                    </button>
                    <a href="https://www.pinterest.com/${escapeHtml(acc.username)}/" target="_blank" rel="noopener noreferrer" class="text-muted-foreground/60 hover:text-primary transition-colors p-0.5" title="View on Pinterest">
                      <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 13v6a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2h6"/><polyline points="15 3 21 3 21 9"/><line x1="10" y1="14" x2="21" y2="3"/></svg>
                    </a>
                    ${compBadge}
                    <span class="text-[9.5px] font-bold uppercase px-1.5 py-0.2 rounded-full ${badgeClass}">
                      ${badgeText}
                    </span>
                  </div>
                </div>
              </div>
            </td>
            ${dynamicCells}
            <td class="${padClass} text-center">
              <label class="relative inline-flex items-center cursor-pointer shrink-0">
                <input
                  type="checkbox"
                  data-acc-ingest-toggle="${escapeHtml(acc.id)}"
                  ${acc.ingest_enabled !== false && acc.status === 'active' ? 'checked' : ''}
                  class="sr-only peer"
                  id="toggle-ingest-${escapeHtml(acc.id)}"
                />
                <div class="w-9 h-5 bg-zinc-300 dark:bg-zinc-700 peer-focus:outline-none rounded-full peer peer-checked:after:translate-x-4 peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:rounded-full after:h-4 after:w-4 after:transition-all after:shadow-xs peer-checked:bg-emerald-500"></div>
              </label>
            </td>
            <td class="${padClass} text-right">
              <div class="inline-flex items-center justify-end gap-1.5">
                ${isCompetitor ? `
                  <a
                    href="${compHref}"
                    class="inline-flex items-center justify-center h-7 w-7 rounded-xl border border-blue-500/30 bg-blue-500/10 text-blue-600 dark:text-blue-400 hover:bg-blue-500/20 transition-colors shadow-2xs"
                    title="View @${escapeHtml(acc.username)} in Competitor Intelligence"
                  >
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="22" y1="12" x2="18" y2="12"/><line x1="6" y1="12" x2="2" y2="12"/><line x1="12" y1="6" x2="12" y2="2"/><line x1="12" y1="22" x2="12" y2="18"/></svg>
                  </a>
                ` : `
                  <button
                    type="button"
                    data-send-to-comp-user="${escapeHtml(acc.username)}"
                    class="inline-flex items-center justify-center h-7 w-7 rounded-xl border border-border bg-background hover:border-emerald-500/50 hover:bg-emerald-500/10 text-muted-foreground hover:text-emerald-600 dark:hover:text-emerald-400 transition-colors cursor-pointer shadow-2xs"
                    title="Track in Competitor Intelligence"
                  >
                    <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="22" y1="12" x2="18" y2="12"/><line x1="6" y1="12" x2="2" y2="12"/><line x1="12" y1="6" x2="12" y2="2"/><line x1="12" y1="22" x2="12" y2="18"/></svg>
                  </button>
                `}
                <a
                  href="/pinarchive/account/${encodeURIComponent(acc.username)}"
                  class="inline-flex items-center gap-1 rounded-xl border border-border bg-background hover:bg-muted/80 text-foreground px-2.5 py-1 text-xs font-semibold shadow-2xs hover:border-primary/40 transition-all"
                >
                  <span>Details</span>
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3 text-muted-foreground" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
                </a>
                <button
                  type="button"
                  data-single-delete-acc-id="${escapeHtml(acc.id)}"
                  data-single-delete-acc-user="${escapeHtml(acc.username)}"
                  class="inline-flex items-center justify-center h-7 w-7 rounded-xl border border-border bg-background hover:border-rose-500/50 hover:bg-rose-500/10 text-muted-foreground hover:text-rose-500 transition-colors cursor-pointer shadow-2xs"
                  title="Delete account"
                >
                  <svg xmlns="http://www.w3.org/2000/svg" class="h-3 w-3" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="3 6 5 6 21 6"/><path d="M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/><line x1="10" y1="11" x2="10" y2="17"/><line x1="14" y1="11" x2="14" y2="17"/></svg>
                </button>
              </div>
            </td>
          </tr>
        `;
      })
      .join('');

    attachAccountSelectionListeners();
    attachAccountIngestToggleListeners();
    attachSingleDeleteListeners();
    attachIntervalChangeListeners();
    attachSendToCompetitorListeners();

    // Copy handle listeners
    tbody.querySelectorAll<HTMLElement>('[data-pa-copy-handle]').forEach(btn => {
      btn.addEventListener('click', (e) => {
        e.stopPropagation();
        copyToClipboard(btn.dataset.paCopyHandle!, btn);
      });
    });
  }

  // ─── Density & Number Format Switches ────────────────────────────────────
  const densityToggleBtn = document.getElementById('pa-density-toggle-btn');
  const densityLabel = document.getElementById('pa-density-label');
  const numFormatToggleBtn = document.getElementById('pa-num-format-toggle-btn');
  const numFormatLabel = document.getElementById('pa-num-format-label');

  function updateDensityUI() {
    if (densityLabel) densityLabel.textContent = isCompactDensity ? 'Compact' : 'Comfortable';
  }
  densityToggleBtn?.addEventListener('click', () => {
    isCompactDensity = !isCompactDensity;
    localStorage.setItem('pa_density', isCompactDensity ? 'compact' : 'comfortable');
    updateDensityUI();
    filterAndRenderAccounts();
  });
  updateDensityUI();

  function updateNumFormatUI() {
    if (numFormatLabel) numFormatLabel.textContent = isCompactNumbers ? 'Compact' : 'Full';
  }
  numFormatToggleBtn?.addEventListener('click', () => {
    isCompactNumbers = !isCompactNumbers;
    localStorage.setItem('pa_num_fmt', isCompactNumbers ? 'compact' : 'full');
    updateNumFormatUI();
    filterAndRenderAccounts();
  });
  updateNumFormatUI();

  // ─── Status Tabs & Search Filters ─────────────────────────────────────────
  const statusTabBtns = document.querySelectorAll<HTMLButtonElement>('.pa-status-tab-btn');
  function syncStatusTabClasses() {
    statusTabBtns.forEach(b => {
      const tabKey = b.dataset.statusTab || 'all';
      const active = tabKey === activeStatusTab;
      b.className = active
        ? 'pa-status-tab-btn rounded-lg px-3 py-1.5 text-xs font-semibold bg-background text-foreground shadow-2xs transition-all whitespace-nowrap'
        : 'pa-status-tab-btn rounded-lg px-3 py-1.5 text-xs font-semibold text-muted-foreground hover:text-foreground transition-all whitespace-nowrap';
    });
  }
  statusTabBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      activeStatusTab = (btn.dataset.statusTab as any) || 'all';
      syncStatusTabClasses();
      filterAndRenderAccounts();
    });
  });

  const searchInput = document.getElementById('account-search-input') as HTMLInputElement | null;
  const searchClearBtn = document.getElementById('pa-search-clear-btn');
  const intervalFilterSelect = document.getElementById('pa-interval-filter') as HTMLSelectElement | null;

  searchInput?.addEventListener('input', () => {
    accountSearchTerm = searchInput.value.trim();
    if (accountSearchTerm) searchClearBtn?.classList.remove('hidden');
    else searchClearBtn?.classList.add('hidden');
    filterAndRenderAccounts();
  });

  searchClearBtn?.addEventListener('click', () => {
    if (searchInput) searchInput.value = '';
    accountSearchTerm = '';
    searchClearBtn?.classList.add('hidden');
    filterAndRenderAccounts();
  });

  intervalFilterSelect?.addEventListener('change', () => {
    selectedInterval = intervalFilterSelect.value;
    filterAndRenderAccounts();
  });

  // ─── Overview & KPIs ──────────────────────────────────────────────────────
  async function loadOverview(retries = 2) {
    const kpiAccounts = document.getElementById('kpi-accounts-count');
    const kpiArchived = document.getElementById('kpi-archived-pins');
    const kpiSaves = document.getElementById('kpi-total-saves');
    const kpiShares = document.getElementById('kpi-total-shares');
    const metaText = document.getElementById('refresh-meta-text');
    const tbody = document.getElementById('accounts-table-body');

    try {
      const root = document.getElementById('pinarchive-page-root');
      const wsId = root?.getAttribute('data-workspace-id');
      const url = wsId ? `/api/pinarchive/overview?workspace_id=${encodeURIComponent(wsId)}` : '/api/pinarchive/overview';

      const [res] = await Promise.all([
        fetch(url, { signal: AbortSignal.timeout(8000) }),
        loadCompetitorUsernames(),
      ]);
      if (!res.ok) throw new Error(`HTTP ${res.status}`);
      const json = await res.json();
      if (!json.success) throw new Error(json.error || 'Failed to load overview');

      const totals = json.totals || {};
      const accounts = json.accounts || [];
      rawAccounts = accounts;

      // Update KPI cards
      if (kpiAccounts) kpiAccounts.textContent = fmtNumberFull(totals.accounts ?? accounts.length);
      if (kpiArchived) kpiArchived.textContent = fmtNumberFull(totals.archived_pins ?? 0);
      if (kpiSaves) kpiSaves.textContent = fmtNumberFull(totals.sum_saves ?? 0);
      if (kpiShares) kpiShares.textContent = fmtNumberFull(totals.sum_shares ?? 0);

      // Meta refresh line
      if (metaText) {
        if (accounts.length > 0 && accounts[0].last_run_at) {
          metaText.textContent = `Last refreshed ${timeAgo(accounts[0].last_run_at)}`;
        } else {
          metaText.textContent = 'No sync runs recorded';
        }
      }

      renderTableHeader();
      filterAndRenderAccounts();
      updateCostEstimate();
    } catch (e: any) {
      if (retries > 0) {
        if (tbody) tbody.innerHTML = `<tr><td colspan="12" class="p-8 text-center text-xs text-muted-foreground animate-pulse">Retrying… (attempt ${3 - retries}/2)</td></tr>`;
        setTimeout(() => loadOverview(retries - 1), 1500);
        return;
      }
      console.error('Error loading PinArchive overview:', e);

      if (kpiAccounts) kpiAccounts.textContent = '—';
      if (kpiArchived) kpiArchived.textContent = '—';
      if (kpiSaves) kpiSaves.textContent = '—';
      if (kpiShares) kpiShares.textContent = '—';
      if (metaText) metaText.textContent = 'Sync schedule unavailable';

      if (tbody) tbody.innerHTML =
        `<tr><td colspan="12" class="p-8 text-center text-xs text-rose-500">
           Failed to load accounts. <button id="retry-load-btn" class="underline font-bold cursor-pointer ml-1">Retry</button>
         </td></tr>`;
      document.getElementById('retry-load-btn')?.addEventListener('click', () => {
        if (tbody) tbody.innerHTML = `<tr><td colspan="12" class="p-8 text-center text-xs text-muted-foreground animate-pulse">Loading tracked accounts…</td></tr>`;
        loadOverview(2);
      });
    }
  }

  // ─── Settings Panel Logic ─────────────────────────────────────────────────
  let canAdminSettings = true;

  function updateCostEstimate() {
    const transparencyEl = document.getElementById('transparency-cost-estimate');
    if (!transparencyEl) return;
    const accCount = rawAccounts.length;
    const stopPagesInp = document.getElementById('setting-discovery-stop-pages') as HTMLInputElement | null;
    const stopPages = parseInt(stopPagesInp?.value || '3', 10) || 3;
    const pagesPerDay = accCount * stopPages;
    const candidatesPerDay = pagesPerDay * 50;
    transparencyEl.textContent = `≈ ${fmtNumberFull(pagesPerDay)} pages/day, ≈ ${fmtNumberFull(candidatesPerDay)} pins/day (${accCount} accounts × ${stopPages} pages)`;
  }

  async function loadSettings(retries = 1) {
    try {
      const res = await fetch('/api/pinarchive/settings', { signal: AbortSignal.timeout(8000) });
      if (res.status === 401 || res.status === 403) {
        canAdminSettings = false;
        disableSettingsForm();
        return;
      }
      if (!res.ok) throw new Error(`HTTP ${res.status}`);
      const json = await res.json();
      if (!json.success) return;

      const ingestCb = document.getElementById('setting-ingest-enabled') as HTMLInputElement | null;
      const ingestLabel = document.getElementById('setting-ingest-label');
      const policySel = document.getElementById('setting-paused-policy') as HTMLSelectElement | null;
      const maxBatchInp = document.getElementById('setting-max-batch') as HTMLInputElement | null;
      const minSavesInp = document.getElementById('setting-min-saves') as HTMLInputElement | null;
      const minRepinsInp = document.getElementById('setting-min-repins') as HTMLInputElement | null;
      const risingAgeInp = document.getElementById('setting-rising-age') as HTMLInputElement | null;
      const risingSavesInp = document.getElementById('setting-rising-saves') as HTMLInputElement | null;
      const refreshMaxPinsInp = document.getElementById('setting-refresh-max-pins') as HTMLInputElement | null;
      const discoveryMaxInp = document.getElementById('setting-discovery-max-pages') as HTMLInputElement | null;
      const discoveryStopInp = document.getElementById('setting-discovery-stop-pages') as HTMLInputElement | null;
      const auditSweepCb = document.getElementById('setting-audit-sweep-enabled') as HTMLInputElement | null;
      const auditSweepLabel = document.getElementById('setting-audit-sweep-label');
      const dailySheetSyncCb = document.getElementById('setting-daily-sheet-sync') as HTMLInputElement | null;
      const dailySheetSyncLabel = document.getElementById('setting-daily-sheet-sync-label');
      const summaryBadge = document.getElementById('settings-summary-badge');

      if (ingestCb) {
        ingestCb.checked = Boolean(json.ingest_enabled);
        if (ingestLabel) {
          ingestLabel.textContent = ingestCb.checked ? 'Active' : 'Disabled';
          ingestLabel.className = `text-[11px] font-bold ${ingestCb.checked ? 'text-emerald-500' : 'text-rose-500'}`;
        }
        if (summaryBadge) {
          summaryBadge.textContent = ingestCb.checked ? '🟢 Ingest Active' : '🔴 Ingest Paused';
          summaryBadge.className = `text-[10px] font-bold px-2 py-0.2 rounded-full border ${
            ingestCb.checked
              ? 'bg-emerald-500/10 text-emerald-600 dark:text-emerald-400 border-emerald-500/20'
              : 'bg-rose-500/10 text-rose-600 dark:text-rose-400 border-rose-500/20'
          }`;
        }
      }
      if (policySel) policySel.value = json.paused_account_policy || 'reject';
      if (maxBatchInp) maxBatchInp.value = String(json.max_batch_pins || 500);
      if (minSavesInp) minSavesInp.value = String(json.pin_filter_min_saves ?? 0);
      if (minRepinsInp) minRepinsInp.value = String(json.pin_filter_min_repins ?? 0);
      if (risingAgeInp) risingAgeInp.value = String(json.pin_filter_rising_age_days ?? 14);
      if (risingSavesInp) risingSavesInp.value = String(json.pin_filter_rising_saves ?? 34);
      if (refreshMaxPinsInp) refreshMaxPinsInp.value = String(json.refresh_max_pins ?? 0);
      const refreshMinSavesInp = document.getElementById('setting-refresh-min-saves') as HTMLInputElement | null;
      if (refreshMinSavesInp) refreshMinSavesInp.value = String(json.refresh_min_saves ?? 0);
      if (discoveryMaxInp) discoveryMaxInp.value = String(json.discovery_max_pages ?? 50);
      if (discoveryStopInp) discoveryStopInp.value = String(json.discovery_stop_pages ?? 3);
      if (auditSweepCb) {
        auditSweepCb.checked = json.audit_sweep_enabled !== false;
        if (auditSweepLabel) {
          auditSweepLabel.textContent = auditSweepCb.checked ? 'Active' : 'Off';
          auditSweepLabel.className = `text-xs font-bold ${auditSweepCb.checked ? 'text-emerald-500' : 'text-muted-foreground'}`;
        }
      }
      const githubScheduleCb = document.getElementById('setting-github-schedule') as HTMLInputElement | null;
      const githubScheduleLabel = document.getElementById('setting-github-schedule-label');
      const ghStatusDot = document.getElementById('pipeline-gh-status-dot');
      const ghStatusText = document.getElementById('pipeline-gh-status-text');

      if (githubScheduleCb) {
        githubScheduleCb.checked = json.github_schedule_enabled !== false;
        if (githubScheduleLabel) {
          githubScheduleLabel.textContent = githubScheduleCb.checked ? 'Active' : 'Off';
          githubScheduleLabel.className = `text-xs font-bold ${githubScheduleCb.checked ? 'text-emerald-500' : 'text-muted-foreground'}`;
        }
        if (ghStatusDot && ghStatusText) {
          if (githubScheduleCb.checked) {
            ghStatusDot.className = 'h-1.5 w-1.5 rounded-full bg-primary';
            ghStatusText.innerHTML = 'Daily Pipeline: <strong class="text-foreground">Daily @ 07:00 UTC</strong>';
          } else {
            ghStatusDot.className = 'h-1.5 w-1.5 rounded-full bg-amber-500';
            ghStatusText.innerHTML = 'Daily Pipeline: <strong class="text-amber-500">07:00 UTC (Off · FastCron Only)</strong>';
          }
        }
      }

      if (dailySheetSyncCb) {
        dailySheetSyncCb.checked = Boolean(json.daily_sheet_sync_enabled);
        if (dailySheetSyncLabel) {
          dailySheetSyncLabel.textContent = dailySheetSyncCb.checked ? 'Active' : 'Off';
          dailySheetSyncLabel.className = `text-xs font-bold ${dailySheetSyncCb.checked ? 'text-emerald-500' : 'text-muted-foreground'}`;
        }
      }

      updateCostEstimate();
      updateLogicBar();
    } catch (e) {
      if (retries > 0) {
        setTimeout(() => loadSettings(retries - 1), 1500);
        return;
      }
      console.error('Failed to load settings:', e);
    }
  }

  function updateLogicBar() {
    const minSaves = parseInt((document.getElementById('setting-min-saves') as HTMLInputElement)?.value || '0', 10);
    const minRepins = parseInt((document.getElementById('setting-min-repins') as HTMLInputElement)?.value || '0', 10);
    const risingAge = parseInt((document.getElementById('setting-rising-age') as HTMLInputElement)?.value || '14', 10);
    const risingSaves = parseInt((document.getElementById('setting-rising-saves') as HTMLInputElement)?.value || '20', 10);
    const logicBar = document.getElementById('pinarchive-logic-bar');
    if (!logicBar) return;

    const parts = [];
    if (minSaves > 0) parts.push(`≥${minSaves} saves`);
    if (minRepins > 0) parts.push(`≥${minRepins} repins`);
    if (risingAge > 0 && risingSaves > 0) parts.push(`<${risingAge}d & ≥${risingSaves} saves`);

    const summaryStr = parts.length > 0 ? parts.join(' OR ') : 'Active rules';
    const descEl = logicBar.querySelector('p');
    if (descEl) {
      descEl.innerHTML = `<strong class="text-primary font-bold">OR Condition Active (${summaryStr}):</strong> Any pin matching <span class="underline decoration-primary/50 underline-offset-2 font-bold">ANY single rule above</span> will be automatically preserved, ranked, and synced to Supabase &amp; Google Sheets.`;
    }
  }

  function setupLogicBarListeners() {
    const inputs = document.querySelectorAll('#setting-min-saves, #setting-min-repins, #setting-rising-age, #setting-rising-saves');
    inputs.forEach(el => {
      el.addEventListener('input', updateLogicBar);
    });
  }

  function setupToggleSwitchListeners() {
    const auditSweepCb = document.getElementById('setting-audit-sweep-enabled') as HTMLInputElement | null;
    const auditSweepLabel = document.getElementById('setting-audit-sweep-label');
    auditSweepCb?.addEventListener('change', () => {
      if (auditSweepLabel) {
        auditSweepLabel.textContent = auditSweepCb.checked ? 'Active' : 'Off';
        auditSweepLabel.className = `text-xs font-bold ${auditSweepCb.checked ? 'text-emerald-500' : 'text-muted-foreground'}`;
      }
    });

    const githubScheduleCb = document.getElementById('setting-github-schedule') as HTMLInputElement | null;
    const githubScheduleLabel = document.getElementById('setting-github-schedule-label');
    const ghStatusDot = document.getElementById('pipeline-gh-status-dot');
    const ghStatusText = document.getElementById('pipeline-gh-status-text');
    githubScheduleCb?.addEventListener('change', () => {
      if (githubScheduleLabel) {
        githubScheduleLabel.textContent = githubScheduleCb.checked ? 'Active' : 'Off';
        githubScheduleLabel.className = `text-xs font-bold ${githubScheduleCb.checked ? 'text-emerald-500' : 'text-muted-foreground'}`;
      }
      if (ghStatusDot && ghStatusText) {
        if (githubScheduleCb.checked) {
          ghStatusDot.className = 'h-2 w-2 rounded-full bg-primary';
          ghStatusText.textContent = 'Daily @ 07:00 UTC';
        } else {
          ghStatusDot.className = 'h-2 w-2 rounded-full bg-amber-500';
          ghStatusText.textContent = 'Off (FastCron Only)';
        }
      }
    });

    const dailySheetSyncCb = document.getElementById('setting-daily-sheet-sync') as HTMLInputElement | null;
    const dailySheetSyncLabel = document.getElementById('setting-daily-sheet-sync-label');
    dailySheetSyncCb?.addEventListener('change', () => {
      if (dailySheetSyncLabel) {
        dailySheetSyncLabel.textContent = dailySheetSyncCb.checked ? 'Active' : 'Off';
        dailySheetSyncLabel.className = `text-xs font-bold ${dailySheetSyncCb.checked ? 'text-emerald-500' : 'text-muted-foreground'}`;
      }
    });

    const masterIngestCb = document.getElementById('setting-ingest-enabled') as HTMLInputElement | null;
    const masterIngestLabel = document.getElementById('setting-ingest-label');
    masterIngestCb?.addEventListener('change', () => {
      if (masterIngestLabel) {
        masterIngestLabel.textContent = masterIngestCb.checked ? 'Active' : 'Paused';
        masterIngestLabel.className = `text-[11px] font-bold ${masterIngestCb.checked ? 'text-emerald-500' : 'text-rose-500'}`;
      }
    });
  }

  function setupSettingsCollapse() {
    const toggleBtn = document.getElementById('toggle-settings-collapse-btn');
    const panelBody = document.getElementById('settings-panel-body');
    const collapseIcon = document.getElementById('settings-collapse-icon');
    const collapseText = document.getElementById('settings-collapse-text');
    if (!toggleBtn || !panelBody) return;

    let isExpanded = localStorage.getItem('pa_settings_expanded') === 'true';

    function updateCollapseUI() {
      if (isExpanded) {
        panelBody.classList.remove('hidden');
        if (collapseIcon) collapseIcon.style.transform = 'rotate(180deg)';
        if (collapseText) collapseText.textContent = 'Collapse';
      } else {
        panelBody.classList.add('hidden');
        if (collapseIcon) collapseIcon.style.transform = 'rotate(0deg)';
        if (collapseText) collapseText.textContent = 'Configure';
      }
    }

    updateCollapseUI();

    toggleBtn.addEventListener('click', () => {
      isExpanded = !isExpanded;
      localStorage.setItem('pa_settings_expanded', String(isExpanded));
      updateCollapseUI();
    });
  }

  function disableSettingsForm() {
    const saveBtn = document.getElementById('save-ingest-settings-btn') as HTMLButtonElement | null;
    const reevaluateBtn = document.getElementById('reevaluate-candidates-btn') as HTMLButtonElement | null;
    const roleBadge = document.getElementById('settings-role-badge');
    const inputs = document.querySelectorAll<HTMLInputElement | HTMLSelectElement>(
      '#setting-ingest-enabled, #setting-paused-policy, #setting-max-batch, #setting-refresh-max-pins, #setting-refresh-min-saves, #setting-min-saves, #setting-min-repins, #setting-rising-age, #setting-rising-saves, #setting-discovery-max-pages, #setting-discovery-stop-pages, #setting-audit-sweep-enabled, #setting-daily-sheet-sync, #setting-github-schedule'
    );
    inputs.forEach((el) => { el.disabled = true; });
    if (saveBtn) { saveBtn.disabled = true; saveBtn.classList.add('hidden'); }
    if (reevaluateBtn) { reevaluateBtn.disabled = true; reevaluateBtn.classList.add('hidden'); }
    if (roleBadge) roleBadge.classList.remove('hidden');
  }

  async function saveSettings() {
    const ingestCb = document.getElementById('setting-ingest-enabled') as HTMLInputElement | null;
    const policySel = document.getElementById('setting-paused-policy') as HTMLSelectElement | null;
    const maxBatchInp = document.getElementById('setting-max-batch') as HTMLInputElement | null;
    const refreshMaxPinsInp = document.getElementById('setting-refresh-max-pins') as HTMLInputElement | null;
    const refreshMinSavesInp = document.getElementById('setting-refresh-min-saves') as HTMLInputElement | null;
    const minSavesInp = document.getElementById('setting-min-saves') as HTMLInputElement | null;
    const minRepinsInp = document.getElementById('setting-min-repins') as HTMLInputElement | null;
    const risingAgeInp = document.getElementById('setting-rising-age') as HTMLInputElement | null;
    const risingSavesInp = document.getElementById('setting-rising-saves') as HTMLInputElement | null;
    const discoveryMaxInp = document.getElementById('setting-discovery-max-pages') as HTMLInputElement | null;
    const discoveryStopInp = document.getElementById('setting-discovery-stop-pages') as HTMLInputElement | null;
    const auditSweepCb = document.getElementById('setting-audit-sweep-enabled') as HTMLInputElement | null;
    const dailySheetSyncCb = document.getElementById('setting-daily-sheet-sync') as HTMLInputElement | null;
    const githubScheduleCb = document.getElementById('setting-github-schedule') as HTMLInputElement | null;
    const statusMsg = document.getElementById('settings-status-msg');
    const saveBtn = document.getElementById('save-ingest-settings-btn') as HTMLButtonElement | null;
    const summaryBadge = document.getElementById('settings-summary-badge');

    if (!ingestCb || !policySel || !maxBatchInp || !minSavesInp || !minRepinsInp || !risingAgeInp || !risingSavesInp) return;

    if (saveBtn) saveBtn.disabled = true;
    if (statusMsg) {
      statusMsg.textContent = 'Saving...';
      statusMsg.className = 'text-xs font-semibold text-muted-foreground';
    }

    try {
      const res = await fetch('/api/pinarchive/settings', {
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          ingest_enabled: ingestCb.checked,
          paused_account_policy: policySel.value,
          max_batch_pins: parseInt(maxBatchInp.value, 10) || 500,
          refresh_max_pins: parseInt(refreshMaxPinsInp?.value || '0', 10) || 0,
          refresh_min_saves: parseInt(refreshMinSavesInp?.value || '0', 10) || 0,
          pin_filter_min_saves: parseInt(minSavesInp.value, 10) || 0,
          pin_filter_min_repins: parseInt(minRepinsInp.value, 10) || 0,
          pin_filter_rising_age_days: Number.isInteger(parseInt(risingAgeInp.value, 10)) ? parseInt(risingAgeInp.value, 10) : 14,
          pin_filter_rising_saves: Number.isInteger(parseInt(risingSavesInp.value, 10)) ? parseInt(risingSavesInp.value, 10) : 34,
          discovery_max_pages: parseInt(discoveryMaxInp?.value || '50', 10) || 50,
          discovery_stop_pages: parseInt(discoveryStopInp?.value || '3', 10) || 3,
          audit_sweep_enabled: auditSweepCb ? auditSweepCb.checked : true,
          daily_sheet_sync_enabled: dailySheetSyncCb ? dailySheetSyncCb.checked : false,
          github_schedule_enabled: githubScheduleCb ? githubScheduleCb.checked : true,
        }),
      });

      if (res.status === 403) {
        canAdminSettings = false;
        disableSettingsForm();
        if (statusMsg) {
          statusMsg.textContent = 'Forbidden: Admin access required.';
          statusMsg.className = 'text-xs font-semibold text-rose-500';
        }
        return;
      }

      const json = await res.json();
      if (!res.ok || !json.success) throw new Error(json.error || 'Failed to save settings');

      if (summaryBadge) {
        summaryBadge.textContent = ingestCb.checked ? '🟢 Ingest Active' : '🔴 Ingest Paused';
        summaryBadge.className = `text-[10px] font-bold px-2 py-0.2 rounded-full border ${
          ingestCb.checked
            ? 'bg-emerald-500/10 text-emerald-600 dark:text-emerald-400 border-emerald-500/20'
            : 'bg-rose-500/10 text-rose-600 dark:text-rose-400 border-rose-500/20'
        }`;
      }

      updateCostEstimate();

      if (statusMsg) {
        statusMsg.textContent = 'Settings saved!';
        statusMsg.className = 'text-xs font-semibold text-emerald-500';
        setTimeout(() => { if (statusMsg) statusMsg.textContent = ''; }, 3000);
      }
    } catch (e: any) {
      if (statusMsg) {
        statusMsg.textContent = e.message || 'Save failed';
        statusMsg.className = 'text-xs font-semibold text-rose-500';
      }
    } finally {
      if (saveBtn && canAdminSettings) saveBtn.disabled = false;
    }
  }

  function setupReevaluateButton() {
    const btn = document.getElementById('reevaluate-candidates-btn') as HTMLButtonElement | null;
    const icon = document.getElementById('reevaluate-icon');
    const text = document.getElementById('reevaluate-btn-text');
    if (!btn) return;

    btn.addEventListener('click', async () => {
      const root = document.getElementById('pinarchive-page-root');
      const workspaceId = root?.getAttribute('data-workspace-id') || '';
      if (!workspaceId) {
        toast('Workspace identifier missing', 'error');
        return;
      }

      const confirmed = confirm(
        'Re-evaluate Candidate Pins?\n\nThis will scan all candidate pins against the active qualification rules (saves, repins, velocity) and synchronize qualified pins to Google Sheets. Do you want to proceed?'
      );
      if (!confirmed) return;

      btn.disabled = true;
      if (icon) icon.classList.add('animate-spin');
      if (text) text.textContent = 'Re-evaluating...';

      try {
        const res = await fetch('/api/internal/pinarchive/reevaluate', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ workspace_id: workspaceId }),
        });

        const json = await res.json().catch(() => ({}));
        if (res.ok && json.success) {
          const synced = json.sheet_synced ?? 0;
          const promoted = json.promoted ?? 0;
          toast(`Re-evaluation complete! (${synced} synced from Sheet, ${promoted} promoted)`, 'success');
          loadOverview(false as any);
        } else if (res.status === 401 || res.status === 403) {
          toast('Admin access required to trigger re-evaluation', 'error');
        } else {
          toast(json.error || 'Failed to trigger re-evaluation', 'error');
        }
      } catch (err: any) {
        toast(err?.message || 'Network error triggering re-evaluation', 'error');
      } finally {
        if (icon) icon.classList.remove('animate-spin');
        if (text) text.textContent = 'Re-evaluate Candidates';
        btn.disabled = false;
      }
    });
  }

  // ─── Account Ingest Toggle & Selection ────────────────────────────────────
  function attachAccountIngestToggleListeners() {
    document.querySelectorAll<HTMLInputElement>('[data-acc-ingest-toggle]').forEach((cb) => {
      cb.addEventListener('change', async () => {
        const id = cb.getAttribute('data-acc-ingest-toggle');
        if (!id) return;
        const enabled = cb.checked;
        cb.disabled = true;

        const root = document.getElementById('pinarchive-page-root');
        const wsId = root?.getAttribute('data-workspace-id');

        try {
          const res = await fetch('/api/pinarchive/accounts-ingest-toggle', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
              workspace_id: wsId,
              account_ids: [id],
              ingest_enabled: enabled,
            }),
          });

          if (res.status === 403) {
            cb.checked = !enabled;
            toast('Admin access required to toggle account ingestion.', 'error');
            return;
          }

          const json = await res.json();
          if (!res.ok || !json.success) {
            cb.checked = !enabled;
            toast(json.error || 'Failed to update account ingest status.', 'error');
          } else {
            const target = rawAccounts.find(a => a.id === id);
            if (target) {
              target.ingest_enabled = enabled;
              target.status = enabled ? 'active' : 'paused';
            }
            toast(`@${target?.username || 'Account'} is now ${enabled ? 'active' : 'paused'}`);
            filterAndRenderAccounts();
          }
        } catch (e: any) {
          cb.checked = !enabled;
          toast(e.message || 'Error updating account status', 'error');
        } finally {
          cb.disabled = false;
        }
      });
    });
  }

  function updateSelectedAccountsUI() {
    const actionBar = document.getElementById('accounts-action-bar');
    const selectedLabel = document.getElementById('accounts-selected-label');
    const count = selectedAccounts.size;

    if (count > 0) {
      if (actionBar) {
        actionBar.classList.remove('hidden');
        actionBar.classList.add('flex');
      }
      if (selectedLabel) selectedLabel.textContent = `${count} selected`;
    } else {
      if (actionBar) {
        actionBar.classList.add('hidden');
        actionBar.classList.remove('flex');
      }
    }
  }

  function attachAccountSelectionListeners() {
    document.querySelectorAll<HTMLInputElement>('input[data-acc-select]').forEach((cb) => {
      cb.addEventListener('change', () => {
        const id = cb.getAttribute('data-acc-select');
        if (!id) return;
        if (cb.checked) selectedAccounts.add(id);
        else selectedAccounts.delete(id);
        updateSelectedAccountsUI();
      });
    });
  }

  function attachIntervalChangeListeners() {
    document.querySelectorAll<HTMLSelectElement>('.interval-select').forEach((sel) => {
      sel.addEventListener('change', async () => {
        const username = sel.getAttribute('data-username');
        const accountId = sel.getAttribute('data-account-id');
        const prevDays = sel.getAttribute('data-prev-days');
        const newDays = parseInt(sel.value, 10);
        if (!newDays) return;

        sel.disabled = true;
        try {
          const root = document.getElementById('pinarchive-page-root');
          const wsId = root?.getAttribute('data-workspace-id');
          const res = await fetch('/api/pinarchive/accounts-interval', {
            method: 'PATCH',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
              workspace_id: wsId,
              account_id: accountId || undefined,
              username: username || undefined,
              interval_days: newDays,
            }),
          });
          const json = await res.json();
          if (!res.ok || !json.success) {
            sel.value = prevDays || '3';
            toast(json.error || 'Failed to update interval', 'error');
          } else {
            sel.setAttribute('data-prev-days', String(newDays));
            // Update in local array
            const target = rawAccounts.find(a => (accountId && a.id === accountId) || (username && a.username === username));
            if (target) {
              target.interval_days = newDays;
              if (json.next_run_dates && (json.next_run_dates[target.id] || json.next_run_dates[target.username])) {
                target.next_run_at = json.next_run_dates[target.id] || json.next_run_dates[target.username];
              }
            }
            toast(`Discovery interval for @${username || 'account'} set to ${newDays}d`);
            renderTable();
          }
        } catch (e: any) {
          sel.value = prevDays || '3';
          toast(e.message || 'Error updating interval', 'error');
        } finally {
          sel.disabled = false;
        }
      });
    });
  }

  // ─── Send to Competitor Intelligence (Single Row) ─────────────────────────
  function attachSendToCompetitorListeners() {
    document.querySelectorAll<HTMLButtonElement>('[data-send-to-comp-user]').forEach((btn) => {
      btn.addEventListener('click', async (e) => {
        e.stopPropagation();
        const username = btn.getAttribute('data-send-to-comp-user');
        if (!username) return;

        const confirmed = confirm(`Track @${username} in Competitor Intelligence?`);
        if (!confirmed) return;

        const root = document.getElementById('pinarchive-page-root');
        const wsId = root?.getAttribute('data-workspace-id');

        btn.disabled = true;
        try {
          const res = await fetch('/api/admin/competitors', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
              workspace_id: wsId,
              username,
              account_type: 'competitor',
            }),
          });
          const json = await res.json();
          if (res.ok && json.success) {
            toast(`Sent @${username} to Competitor Intelligence & dispatched scraper!`);
            const compId = json.competitor?.id || json.competitors?.[0]?.id;
            // Dispatch immediate scraping for this competitor
            fetch('/api/admin/competitor-ops', {
              method: 'POST',
              headers: { 'Content-Type': 'application/json' },
              body: JSON.stringify({
                action: 'dispatch',
                workspace_id: wsId,
                competitor_id: compId || undefined,
                target_username: username,
                trigger: 'pinarchive_send',
                force: true,
              }),
            }).catch(console.warn);

            loadOverview(1);
          } else {
            toast(json.error || 'Failed to send account to Competitor Intelligence', 'error');
          }
        } catch (err: any) {
          toast(err.message || 'Network error', 'error');
        } finally {
          btn.disabled = false;
        }
      });
    });
  }

  // ─── Delete Accounts Modal ────────────────────────────────────────────────
  let pendingDeleteIds: string[] = [];

  function openDeleteAccountsModal(ids: string[]) {
    pendingDeleteIds = ids;
    const modal = document.getElementById('delete-accounts-modal');
    const input = document.getElementById('delete-confirm-input') as HTMLInputElement | null;
    const confirmBtn = document.getElementById('confirm-delete-modal-btn') as HTMLButtonElement | null;
    if (!modal) return;

    if (input) input.value = '';
    if (confirmBtn) confirmBtn.disabled = true;

    modal.classList.remove('hidden');
    if (input) input.focus();
  }

  function closeDeleteAccountsModal() {
    const modal = document.getElementById('delete-accounts-modal');
    if (modal) modal.classList.add('hidden');
    pendingDeleteIds = [];
  }

  function attachSingleDeleteListeners() {
    document.querySelectorAll<HTMLButtonElement>('[data-single-delete-acc-id]').forEach((btn) => {
      btn.addEventListener('click', (e) => {
        e.stopPropagation();
        const id = btn.getAttribute('data-single-delete-acc-id');
        if (id) openDeleteAccountsModal([id]);
      });
    });
  }

  // ─── Bulk Action Buttons ──────────────────────────────────────────────────
  function setupBulkActionButtons() {
    document.getElementById('clear-account-selection-btn')?.addEventListener('click', () => {
      selectedAccounts.clear();
      document.querySelectorAll<HTMLInputElement>('input[data-acc-select]').forEach(cb => { cb.checked = false; });
      const selectAll = document.getElementById('select-all-accounts-cb') as HTMLInputElement | null;
      if (selectAll) selectAll.checked = false;
      updateSelectedAccountsUI();
    });

    document.getElementById('delete-selected-accounts-btn')?.addEventListener('click', () => {
      if (selectedAccounts.size === 0) return;
      openDeleteAccountsModal(Array.from(selectedAccounts));
    });

    // Bulk Interval Change Listener
    const bulkIntervalSelect = document.getElementById('bulk-interval-select') as HTMLSelectElement | null;
    bulkIntervalSelect?.addEventListener('change', async () => {
      const newDays = parseInt(bulkIntervalSelect.value, 10);
      if (!newDays || selectedAccounts.size === 0) return;
      const count = selectedAccounts.size;
      const confirmed = confirm(`Change scrape interval to ${newDays} day(s) for ${count} selected account(s)?`);
      if (!confirmed) {
        bulkIntervalSelect.value = '';
        return;
      }

      bulkIntervalSelect.disabled = true;
      try {
        const root = document.getElementById('pinarchive-page-root');
        const wsId = root?.getAttribute('data-workspace-id');
        const res = await fetch('/api/pinarchive/accounts-interval', {
          method: 'PATCH',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            workspace_id: wsId,
            account_ids: Array.from(selectedAccounts),
            interval_days: newDays,
          }),
        });
        const json = await res.json();
        if (res.ok && json.success) {
          toast(`Discovery interval set to ${newDays}d for ${count} account(s)!`);
          rawAccounts.forEach(a => {
            if (selectedAccounts.has(a.id)) {
              a.interval_days = newDays;
            }
          });
          filterAndRenderAccounts();
        } else {
          toast(json.error || 'Failed to update interval', 'error');
        }
      } catch (err: any) {
        toast(err.message || 'Network error', 'error');
      } finally {
        bulkIntervalSelect.disabled = false;
        bulkIntervalSelect.value = '';
      }
    });

    document.getElementById('gas-bulk-run-btn')?.addEventListener('click', async () => {
      if (selectedAccounts.size === 0) return;
      const root = document.getElementById('pinarchive-page-root');
      const wsId = root?.getAttribute('data-workspace-id');
      if (!wsId) {
        toast('Workspace identifier not found', 'error');
        return;
      }

      const selectedUsernames = rawAccounts
        .filter(a => selectedAccounts.has(a.id))
        .map(a => a.username)
        .filter(Boolean);

      if (selectedUsernames.length === 0) {
        toast('No valid accounts selected', 'error');
        return;
      }

      const confirmed = confirm(`Run Pin Discovery on Pinterest for ${selectedUsernames.length} selected account(s)?`);
      if (!confirmed) return;

      const btn = document.getElementById('gas-bulk-run-btn') as HTMLButtonElement | null;
      const originalText = btn?.innerHTML || '<span>🧠 Discover Pins</span>';
      if (btn) {
        btn.disabled = true;
        btn.innerHTML = `<span>⏳ Dispatching…</span>`;
      }

      try {
        const res = await fetch('/api/pinarchive/accounts-gas', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            action: 'run_now',
            workspace_id: wsId,
            usernames: selectedUsernames,
          }),
        });
        const json = await res.json();
        if (res.ok && json.success) {
          const failed = (json.results || []).filter((r: any) => !r.ok);
          if (failed.length > 0) {
            toast(`Discovery queued with warnings: ${failed[0]?.error || 'partial error'}`, 'error');
          } else {
            toast(`🧠 Discovery pipeline dispatched for @${selectedUsernames.join(', @')}!`);
          }
          setTimeout(() => loadOverview(false as any), 2000);
        } else {
          toast(json.error || 'Failed to dispatch discovery pipeline', 'error');
        }
      } catch (err: any) {
        toast(err.message || 'Network error', 'error');
      } finally {
        if (btn) {
          btn.disabled = false;
          btn.innerHTML = originalText;
        }
      }
    });

    document.getElementById('refresh-selected-accounts-btn')?.addEventListener('click', async () => {
      if (selectedAccounts.size === 0) return;
      const root = document.getElementById('pinarchive-page-root');
      const wsId = root?.getAttribute('data-workspace-id');
      if (!wsId) {
        toast('Workspace identifier not found', 'error');
        return;
      }

      const selectedUsernames = rawAccounts
        .filter(a => selectedAccounts.has(a.id))
        .map(a => a.username)
        .filter(Boolean);

      if (selectedUsernames.length === 0) {
        toast('No valid accounts selected', 'error');
        return;
      }

      const confirmed = confirm(`Run Fast Metrics Refresh for ${selectedUsernames.length} selected account(s)?`);
      if (!confirmed) return;

      const icon = document.getElementById('refresh-selected-icon');
      const textSpan = document.getElementById('refresh-selected-btn-text');
      const btn = document.getElementById('refresh-selected-accounts-btn') as HTMLButtonElement | null;

      if (icon) icon.classList.add('animate-spin');
      if (textSpan) textSpan.textContent = 'Syncing…';
      if (btn) btn.disabled = true;

      try {
        const res = await fetch('/api/internal/pinarchive/refresh', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            workspace_id: wsId,
            usernames: selectedUsernames,
          }),
        });
        const json = await res.json();
        if (res.ok && json.success) {
          toast(`⚡ Fast Refresh dispatched for ${selectedUsernames.length} account(s)!`);
          setTimeout(() => loadOverview(false as any), 2000);
        } else {
          toast(json.error || 'Failed to trigger fast refresh', 'error');
        }
      } catch (err: any) {
        toast(err.message || 'Network error', 'error');
      } finally {
        if (icon) icon.classList.remove('animate-spin');
        if (textSpan) textSpan.textContent = '⚡ Fast Refresh';
        if (btn) btn.disabled = false;
      }
    });

    // Send Selected to Competitor Intelligence
    document.getElementById('send-selected-to-competitors-btn')?.addEventListener('click', async () => {
      if (selectedAccounts.size === 0) return;
      const count = selectedAccounts.size;
      const confirmed = confirm(`Send ${count} selected creator account(s) to Competitor Intelligence?`);
      if (!confirmed) return;

      const root = document.getElementById('pinarchive-page-root');
      const wsId = root?.getAttribute('data-workspace-id');

      const selectedUsernames = rawAccounts
        .filter(a => selectedAccounts.has(a.id))
        .map(a => a.username)
        .filter(Boolean);

      if (selectedUsernames.length === 0) return;

      const btn = document.getElementById('send-selected-to-competitors-btn') as HTMLButtonElement | null;
      if (btn) btn.disabled = true;

      try {
        const res = await fetch('/api/admin/competitors', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            workspace_id: wsId,
            competitors: selectedUsernames,
            account_type: 'competitor',
          }),
        });
        const json = await res.json();
        if (res.ok && json.success) {
          toast(`Sent ${json.count || selectedUsernames.length} account(s) to Competitor Intelligence & dispatched scrapers!`);
          const compIds = (json.competitors || []).map((c: any) => c.id).filter(Boolean);
          // Dispatch immediate scraping for the selected competitors
          fetch('/api/admin/competitor-ops', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
              action: 'dispatch',
              workspace_id: wsId,
              ids: compIds,
              trigger: 'pinarchive_bulk_send',
              force: true,
            }),
          }).catch(console.warn);

          loadOverview(1);
        } else {
          toast(json.error || 'Failed to send accounts to Competitor Intelligence', 'error');
        }
      } catch (err: any) {
        toast(err.message || 'Network error', 'error');
      } finally {
        if (btn) btn.disabled = false;
      }
    });

    document.getElementById('gas-bulk-pause-btn')?.addEventListener('click', async () => {
      if (selectedAccounts.size === 0) return;
      const count = selectedAccounts.size;
      const confirmed = confirm(`Pause ingestion for ${count} selected account(s)?`);
      if (!confirmed) return;

      const root = document.getElementById('pinarchive-page-root');
      const wsId = root?.getAttribute('data-workspace-id');

      try {
        const res = await fetch('/api/pinarchive/accounts-ingest-toggle', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            workspace_id: wsId,
            account_ids: Array.from(selectedAccounts),
            ingest_enabled: false,
          }),
        });
        const json = await res.json();
        if (res.ok && json.success) {
          toast(`Paused ingestion for ${count} account(s)`);
          rawAccounts.forEach(a => {
            if (selectedAccounts.has(a.id)) {
              a.ingest_enabled = false;
              a.status = 'paused';
            }
          });
          filterAndRenderAccounts();
        } else {
          toast(json.error || 'Failed to pause accounts', 'error');
        }
      } catch (err: any) {
        toast(err.message || 'Network error', 'error');
      }
    });

    document.getElementById('gas-bulk-resume-btn')?.addEventListener('click', async () => {
      if (selectedAccounts.size === 0) return;
      const count = selectedAccounts.size;
      const confirmed = confirm(`Resume ingestion for ${count} selected account(s)?`);
      if (!confirmed) return;

      const root = document.getElementById('pinarchive-page-root');
      const wsId = root?.getAttribute('data-workspace-id');

      try {
        const res = await fetch('/api/pinarchive/accounts-ingest-toggle', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            workspace_id: wsId,
            account_ids: Array.from(selectedAccounts),
            ingest_enabled: true,
          }),
        });
        const json = await res.json();
        if (res.ok && json.success) {
          toast(`Resumed ingestion for ${count} account(s)`);
          rawAccounts.forEach(a => {
            if (selectedAccounts.has(a.id)) {
              a.ingest_enabled = true;
              a.status = 'active';
            }
          });
          filterAndRenderAccounts();
        } else {
          toast(json.error || 'Failed to resume accounts', 'error');
        }
      } catch (err: any) {
        toast(err.message || 'Network error', 'error');
      }
    });

    // Delete modal inputs
    const confirmInput = document.getElementById('delete-confirm-input') as HTMLInputElement | null;
    const confirmBtn = document.getElementById('confirm-delete-modal-btn') as HTMLButtonElement | null;
    confirmInput?.addEventListener('input', () => {
      if (confirmBtn) confirmBtn.disabled = confirmInput.value.trim().toUpperCase() !== 'DELETE';
    });

    document.getElementById('cancel-delete-modal-btn')?.addEventListener('click', closeDeleteAccountsModal);

    confirmBtn?.addEventListener('click', async () => {
      if (pendingDeleteIds.length === 0) return;
      confirmBtn.disabled = true;
      confirmBtn.textContent = 'Deleting…';

      try {
        const res = await fetch('/api/pinarchive/accounts-delete', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ account_ids: pendingDeleteIds }),
        });
        const json = await res.json();
        if (res.ok && json.success) {
          toast(`Deleted ${pendingDeleteIds.length} account(s)`);
          closeDeleteAccountsModal();
          selectedAccounts.clear();
          loadOverview(false as any);
        } else {
          toast(json.error || 'Failed to delete accounts', 'error');
        }
      } catch (err: any) {
        toast(err.message || 'Network error', 'error');
      } finally {
        if (confirmBtn) {
          confirmBtn.disabled = false;
          confirmBtn.textContent = 'Delete Accounts';
        }
      }
    });

    // Tab switching
    const tabAccounts = document.getElementById('pa-tab-btn-accounts');
    const tabAutomation = document.getElementById('pa-tab-btn-automation');
    const viewAccounts = document.getElementById('pa-tab-view-accounts');
    const viewAutomation = document.getElementById('pa-tab-view-automation');

    tabAccounts?.addEventListener('click', () => {
      tabAccounts.className = 'inline-flex items-center gap-2 px-4 py-2 rounded-xl text-xs font-bold bg-background text-foreground shadow-2xs transition-all cursor-pointer';
      tabAutomation?.classList.remove('bg-background', 'text-foreground', 'shadow-2xs');
      tabAutomation?.classList.add('text-muted-foreground');
      viewAccounts?.classList.remove('hidden');
      viewAutomation?.classList.add('hidden');
    });

    tabAutomation?.addEventListener('click', () => {
      tabAutomation.className = 'inline-flex items-center gap-2 px-4 py-2 rounded-xl text-xs font-bold bg-background text-foreground shadow-2xs transition-all cursor-pointer';
      tabAccounts?.classList.remove('bg-background', 'text-foreground', 'shadow-2xs');
      tabAccounts?.classList.add('text-muted-foreground');
      viewAutomation?.classList.remove('hidden');
      viewAccounts?.classList.add('hidden');
    });

    // Top Refresh button
    document.getElementById('pa-refresh-btn')?.addEventListener('click', () => {
      const icon = document.getElementById('pa-refresh-icon');
      icon?.classList.add('animate-spin');
      loadOverview(1).finally(() => {
        setTimeout(() => icon?.classList.remove('animate-spin'), 400);
      });
    });

    // ─── Architecture Guide Modal Controls ────────────────────────────────────
    function setupArchitectureGuideModal() {
      const modal = document.getElementById('architecture-guide-modal');
      const openBtn = document.getElementById('open-architecture-guide-btn');
      const bannerBtn = document.getElementById('open-architecture-guide-banner-btn');
      const closeBtn = document.getElementById('close-architecture-guide-modal-btn');
      const doneBtn = document.getElementById('done-architecture-guide-modal-btn');
      const backdrop = document.getElementById('architecture-guide-backdrop');

      const tabBtnMaster = document.getElementById('guide-tab-btn-master');
      const tabBtnScheduler = document.getElementById('guide-tab-btn-scheduler');
      const tabBtnMatrix = document.getElementById('guide-tab-btn-matrix');

      const tabViewMaster = document.getElementById('guide-tab-view-master');
      const tabViewScheduler = document.getElementById('guide-tab-view-scheduler');
      const tabViewMatrix = document.getElementById('guide-tab-view-matrix');

      function openModal() {
        modal?.classList.remove('hidden');
        modal?.classList.add('flex');
      }

      function closeModal() {
        modal?.classList.add('hidden');
        modal?.classList.remove('flex');
      }

      openBtn?.addEventListener('click', openModal);
      bannerBtn?.addEventListener('click', openModal);
      closeBtn?.addEventListener('click', closeModal);
      doneBtn?.addEventListener('click', closeModal);
      backdrop?.addEventListener('click', closeModal);

      function switchTab(activeBtn: HTMLElement | null, activeView: HTMLElement | null) {
        [tabBtnMaster, tabBtnScheduler, tabBtnMatrix].forEach(btn => {
          btn?.classList.remove('bg-background', 'text-foreground', 'shadow-2xs');
          btn?.classList.add('text-muted-foreground');
        });
        [tabViewMaster, tabViewScheduler, tabViewMatrix].forEach(view => {
          view?.classList.add('hidden');
        });

        activeBtn?.classList.remove('text-muted-foreground');
        activeBtn?.classList.add('bg-background', 'text-foreground', 'shadow-2xs');
        activeView?.classList.remove('hidden');
      }

      tabBtnMaster?.addEventListener('click', () => switchTab(tabBtnMaster, tabViewMaster));
      tabBtnScheduler?.addEventListener('click', () => switchTab(tabBtnScheduler, tabViewScheduler));
      tabBtnMatrix?.addEventListener('click', () => switchTab(tabBtnMatrix, tabViewMatrix));
    }
    setupArchitectureGuideModal();

    // ─── Add Creator Account Modal Controls ──────────────────────────────────
    function cleanPinterestUsername(input: string): string {
      let u = String(input || '').trim();
      u = u.replace(/^(https?:\/\/)?(www\.)?pinterest\.[a-z.]+\//i, '');
      u = u.replace(/\/.*$/, '');
      u = u.replace(/^@+/, '').trim();
      return u;
    }

    function setupAddCreatorModal() {
      const modal = document.getElementById('add-creator-modal');
      const openBtn = document.getElementById('open-add-creator-modal');
      const closeBtn = document.getElementById('close-add-creator-modal-btn');
      const cancelBtn = document.getElementById('cancel-add-creator-modal-btn');
      const form = document.getElementById('add-creator-form') as HTMLFormElement | null;
      const usernameInput = document.getElementById('new-creator-username') as HTMLInputElement | null;
      const intervalSelect = document.getElementById('new-creator-interval') as HTMLSelectElement | null;
      const syncCheckbox = document.getElementById('sync-to-competitors-checkbox') as HTMLInputElement | null;
      const runDiscoveryCheckbox = document.getElementById('run-discovery-now-checkbox') as HTMLInputElement | null;
      const errorDiv = document.getElementById('add-creator-error');
      const submitBtn = document.getElementById('submit-add-creator-btn') as HTMLButtonElement | null;

      function openModal() {
        if (form) form.reset();
        if (intervalSelect) intervalSelect.value = '1';
        if (syncCheckbox) syncCheckbox.checked = true;
        if (runDiscoveryCheckbox) runDiscoveryCheckbox.checked = true;
        if (errorDiv) {
          errorDiv.textContent = '';
          errorDiv.classList.add('hidden');
        }
        modal?.classList.remove('hidden');
        modal?.classList.add('flex');
        usernameInput?.focus();
      }

      function closeModal() {
        modal?.classList.add('hidden');
        modal?.classList.remove('flex');
      }

      openBtn?.addEventListener('click', openModal);
      closeBtn?.addEventListener('click', closeModal);
      cancelBtn?.addEventListener('click', closeModal);

      form?.addEventListener('submit', async (e) => {
        e.preventDefault();
        if (!usernameInput) return;
        const rawUser = usernameInput.value.trim();
        const cleanUser = cleanPinterestUsername(rawUser);

        if (!cleanUser) {
          if (errorDiv) {
            errorDiv.textContent = 'Please enter a valid Pinterest username or URL.';
            errorDiv.classList.remove('hidden');
          }
          return;
        }

        const root = document.getElementById('pinarchive-page-root');
        const wsId = root?.getAttribute('data-workspace-id');
        const intervalVal = Number(intervalSelect?.value || 1);
        const syncToComp = Boolean(syncCheckbox?.checked);
        const runDiscovery = Boolean(runDiscoveryCheckbox?.checked);

        if (submitBtn) {
          submitBtn.disabled = true;
          submitBtn.textContent = 'Tracking…';
        }
        if (errorDiv) errorDiv.classList.add('hidden');

        try {
          // 1. Import into PinArchive
          const paRes = await fetch('/api/pinarchive/accounts-import', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({
              workspace_id: wsId,
              accounts: [{ username: cleanUser, interval_days: intervalVal }],
              dispatch: false,
            }),
          });
          const paJson = await paRes.json();
          if (!paRes.ok || !paJson.success) {
            throw new Error(paJson.error || 'Failed to add account to PinArchive');
          }

          // 2. Optionally sync to Competitor Intelligence & dispatch immediate scraper
          if (syncToComp) {
            try {
              const compRes = await fetch('/api/admin/competitors', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({
                  workspace_id: wsId,
                  username: cleanUser,
                  account_type: 'competitor',
                }),
              });
              const compJson = await compRes.json();
              const compId = compJson?.competitor?.id || compJson?.competitors?.[0]?.id;

              // Immediately dispatch Competitor Scraper workflow on GitHub Actions
              fetch('/api/admin/competitor-ops', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({
                  action: 'dispatch',
                  workspace_id: wsId,
                  competitor_id: compId || undefined,
                  target_username: cleanUser,
                  trigger: 'creator_modal',
                  force: true,
                }),
              }).catch(console.warn);
            } catch (compErr) {
              console.warn('Competitor sync/dispatch deferred:', compErr);
            }
          }

          // 3. Optionally dispatch initial Discover Pins immediately
          if (runDiscovery) {
            try {
              await fetch('/api/pinarchive/accounts-gas', {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({
                  action: 'run_now',
                  workspace_id: wsId,
                  usernames: [cleanUser],
                }),
              });
            } catch (gasErr) {
              console.warn('Discover pins immediate dispatch deferred:', gasErr);
            }
          }

          const toastDetails = [
            'PinArchive added',
            runDiscovery ? 'Discover Pins dispatched' : null,
            syncToComp ? 'Competitor Scraper dispatched' : null,
          ].filter(Boolean).join(' • ');

          toast(`Started tracking @${cleanUser}! (${toastDetails})`);
          closeModal();
          loadOverview(1);
        } catch (err: any) {
          if (errorDiv) {
            errorDiv.textContent = err.message || 'Error tracking creator';
            errorDiv.classList.remove('hidden');
          }
        } finally {
          if (submitBtn) {
            submitBtn.disabled = false;
            submitBtn.innerHTML = '<span>+ Track Creator</span>';
          }
        }
      });
    }
    setupAddCreatorModal();
  }

  // ─── Keyboard shortcut ────────────────────────────────────────────────────
  document.addEventListener('keydown', (e) => {
    if (e.key === '/' && document.activeElement !== searchInput && !(document.activeElement instanceof HTMLInputElement) && !(document.activeElement instanceof HTMLTextAreaElement)) {
      e.preventDefault();
      searchInput?.focus();
      searchInput?.select();
    }
  });

  // ─── Initialization ───────────────────────────────────────────────────────
  let loadingWatchdogFired = false;
  function armLoadingWatchdog() {
    setTimeout(() => {
      const tbody = document.getElementById('accounts-table-body');
      const stillLoading = tbody && tbody.textContent.includes('Loading tracked accounts');
      const badge = document.getElementById('refresh-meta-text');
      const badgeStuck = badge && badge.textContent.includes('Loading sync schedule');
      if ((stillLoading || badgeStuck) && !loadingWatchdogFired) {
        loadingWatchdogFired = true;
        console.error('[PinArchive] watchdog: loaders did not settle in 30s, forcing error UI');
        if (tbody) tbody.innerHTML =
          `<tr><td colspan="12" class="p-8 text-center text-xs text-rose-500">
             Failed to load accounts. <button id="retry-load-btn" class="underline font-bold cursor-pointer ml-1">Retry</button>
           </td></tr>`;
        document.getElementById('retry-load-btn')?.addEventListener('click', () => {
          loadingWatchdogFired = false;
          if (tbody) tbody.innerHTML = `<tr><td colspan="12" class="p-8 text-center text-xs text-muted-foreground animate-pulse">Loading tracked accounts…</td></tr>`;
          loadOverview(2); armLoadingWatchdog();
        });
        if (badge) badge.textContent = 'Sync schedule unavailable';
      }
    }, 30000);
  }

  function initPinArchivePage() {
    console.info('[PinArchive] init start', { href: location.href, t: new Date().toISOString() });
    const safe = (name: string, fn: () => void) => { try { fn(); } catch (e) { console.error(`[PinArchive] init step failed: ${name}`, e); } };
    safe('buildColVisibilityMenu', buildColVisibilityMenu);
    safe('setupColVisibilityToggle', setupColVisibilityToggle);
    safe('setupSettingsCollapse', setupSettingsCollapse);
    safe('setupLogicBarListeners', setupLogicBarListeners);
    safe('setupToggleSwitchListeners', setupToggleSwitchListeners);
    safe('setupReevaluateButton', setupReevaluateButton);
    safe('setupBulkActionButtons', setupBulkActionButtons);
    safe('saveBtn', () => document.getElementById('save-ingest-settings-btn')?.addEventListener('click', saveSettings));
    loadSettings();
    loadOverview(2);
    armLoadingWatchdog();
  }

  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initPinArchivePage);
  } else {
    initPinArchivePage();
  }
  document.addEventListener('astro:page-load', initPinArchivePage);
</script>
